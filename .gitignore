__pycache__/
*.pyc
slither-env/

# 数据集产物：按数据集分区（products/<数据集>/），raw/ 与 graphs/ 体积大，不纳入版本管理
# （products/*/splits/、products/*/mapping/ 等小文件仍纳入版本管理）
products/**/raw/*
!products/**/raw/filter_report.txt
products/**/graphs/*
!products/**/graphs/ir_cat.json
!products/**/graphs/.gitkeep

# 特征树与图变体（2026-09-19 `decisions.md` §37 起新增的目录形态；2026-09-20 补齐规则）
# ⚠ **此前的规则只覆盖 `graphs/`**，而 §37 之后新增了 `graphs_ft/`（微调编码器特征树）、
#   `graphs_ft_aug/`（② 的对应树）、`graph_variants/`、`src_stage/` 四种形态 ⇒ 它们**不被任何规则匹配**，
#   实测 `git status` 把它们全部列为未跟踪，`git add -A` 会一次性纳入 **约 16 GB**
#   （② `graphs_ft` 单个就 5.0 GB）。同一坑对 SolidiFI（阶段 G）同样成立，故一次补齐。
# ⚠ 写法比 `products/**/graphs/*` 那三条**更绕**，两种更直觉的写法都实测失败过：
#   ① `dir/`（目录式排除）——目录被排除后 `!` 无法取反（AGENTS.md 已记过这条）；
#   ② `dir/*`——会把**子目录本身**也排除，git 不再往里走，于是 `!variant.json` 永远不生效
#      （实测 `graph_variants/<名>/variant.json` 仍被忽略）。
# 可用写法 = **先排除全部内容、再把目录逐个重新纳入**（`!.../**/`，尾斜杠 = 仅目录）。
# 2026-09-21 泛化为 `graphs_ft*`：任务2 新增了 `graphs_ft_buggy/`（含 buggy 的编码器树），
# 字面量规则 `graphs_ft` **匹配不到**它 ⇒ 又是一个同类洞。用通配一次覆盖
# `graphs_ft` / `graphs_ft_aug` / `graphs_ft_buggy` 及今后同前缀的形态。
products/**/graphs_ft*/**
!products/**/graphs_ft*/**/
products/**/graph_variants/**
!products/**/graph_variants/**/
# `variant.json` 是每个变体的**自述文件**（记唯一变量、断言口径、软链/重跑分流），几 KB，
# 是"这个目录里装的到底是什么"的唯一书面证据 ⇒ **必须入库**。
!products/**/variant.json
# DIVE 抽样子集的源文件暂存（21 MB）：可从只读源 `DIVE/Source codes` 按 `sample_seed0.json` 重建
products/**/src_stage/*
# 构建/重跑日志（按步骤打点，审计用）：过程产物，结论已落 `decisions.md` §38
products/dive/*.log
products/*/matrix.log

# 147 MB，超过 GitHub 单文件 100 MB 限制，仅保留在本地，不纳入版本管理
DIVE/Code-based.csv

# 只读数据源，不纳入版本管理（2026-09-14，防「一键提交」误纳入）
# 注：其余 4 个只读源（alldata(readonly)/、DIVE/、SolidiFI/、MVD-HG-dataset/）历史上已入库，
#     此处刻意不跟从该惯例——本目录只读、可由 MVD-HG-dataset 派生重建，无需占版本体积。
alldata_augmentation/

# M5 训练产物：JSON 实验记录（config.json/log.txt/results.json/thresholds.json/summary.json）全部纳入；
# 权重与推理缓存**按"重算指标所需的最小集"纳入**（2026-09-16 起）：
#   best.pt（evaluate.py 的唯一权重输入，4.77 MB/个）+ val_best_probs.pt + test_probs.pt（共 0.3 MB）
#   —— 三者合起来即可**离线重算全部已报告指标、无需重训**；全套实测 115 MB，单文件均 <5 MB。
# last.pt 刻意排除：仅断点续训用、与报告数字无关，却会使体积翻倍（+114 MB）。
runs/**/*.pt
!runs/**/best.pt
!runs/**/val_best_probs.pt
!runs/**/test_probs.pt

# 例外之例外：**作废口径的归档不入库权重**（2026-09-17，decisions §28）。
# `runs/prior_badmetric/` 是坏指标（`.ravel()` 使 micro-F1 退化为 accuracy）口径下选出的权重——
# 它们**不是**"复现所需的最小集"，而是"坏指标如何改变模型选择"的物证，物证价值在 log.txt 的轨迹
# 与 decisions §28 的表格里，不在二进制。与 prior_dropout80（零 .pt 归档）的惯例一致。
# ⚠ 规则顺序敏感：必须写在上面的 `!runs/**/best.pt` **之后**，后者才被覆盖。
# ⚠ 文件仍**留在磁盘上**（未删除，供人工复核）；此处只阻止它们进入版本库。
runs/prior_badmetric/**/best.pt
runs/prior_badmetric/**/last.pt

# 🔴 消融 n=9 的复现研究区：**只入概率缓存、不入权重**（2026-09-21，用户裁定）。
# 目录：`runs/ablation_n9{,_aug}/`（21 臂的非对角 6 对）与 `runs/arch_n9{,_aug}/`
#       （架构基线族 + 参数量匹配对照）。共约 **314 个 run × 4.8 MB ≈ 1.5 GB**，其中
#       `best.pt` 一个文件就占 4.8 MB ⇒ 全入会使入库 `runs/` 由 452 MB 涨到约 2.0 GB（**4 倍**）。
# **为什么这里可以只要缓存**：本区是**复现研究**，回答的是"Δ 的分布与显著性"——
#   全部指标都能由 `test_probs.pt` / `val_best_probs.pt` 离线重算（`collect_ablation_n9.py`
#   与全部审计脚本**只读这两个缓存**，从不加载权重）。
# ⚠ **代价（必须知情）**：没有 `best.pt` 就**不能**从零重跑 `evaluate.py`（它读权重）。
#   可接受的理由：这 21 臂的**对角 3 对仍在 `runs/ablation{,_aug}/<item>/seed{S}/` 且保留 `best.pt`**
#   ——即每个臂仍有 1/3 的配对可完整重跑评测；本区补的只是"另外 6 对"。
# ⚠ 规则顺序敏感：必须写在上面的 `!runs/**/best.pt` **之后**，后者才被覆盖。
# ⚠ 按本仓教训用**前缀通配**而非字面量（`ablation_n9*` 一并覆盖 `_aug`；同形态已踩坑三次）。
runs/ablation_n9*/**/best.pt
runs/arch_n9*/**/best.pt

# 目录：`runs/ablation_buggy/`（**含 buggy_* 的新正典（池 497）上的 21 臂 × 3 种子 = 63 run**，
#       2026-09-21，供 `experiments/per_class_three_caliber_tables_buggy.md` 的消融行）。
# 口径**与上面两组一致**（同一条用户裁定：新跑批只入概率缓存）——理由同样成立：
#   ① 下游（`collect_three_caliber_tables.py` / `error_rates.py`）**只读 `test_probs.pt`**，
#      从不加载权重；② 63 个 `best.pt` 约 **315 MB**，而本区是复现研究。
# ⚠ **代价（知情）**：本组**没有**"对角 3 对另有完整备份"这层兜底（它只有 3 个种子、不是 9 对网格），
#   故这 63 个 run **全部**不能从零重跑 `evaluate.py`。可接受的理由是它与 `per_class_*_buggy.md`
#   一样属于**申报口径的补充读数**，不是论文主表；基线 `runs/buggy_canon/seed{S}/best.pt` **仍在**。
# ⚠ 规则顺序敏感：必须写在上面的 `!runs/**/best.pt` **之后**，后者才被覆盖。
runs/ablation_buggy/**/best.pt

# 目录：`runs/perclass_arm/cap{20,0}/cls_<类名>/seed{0,1,2}/`（**7 个独立二分类器补充臂**，
#       2026-09-23，`scripts/run_perclass_arm.py`；回答「多标签共享是否压制了稀有类」）。
# 口径**与上面几组一致**（同一条用户裁定：补充臂只入概率缓存）——理由同样成立：
#   ① 下游只读 `test_probs.pt` / `val_best_probs.pt`，从不加载权重；
#   ② 7 类 × 3 种子 × 2 个 pos_weight 档 = 42 个 `best.pt`，各约 5 MB ⇒ 约 **210 MB**。
# ⚠ **代价（知情）**：本组不能从零重跑 `evaluate.py`。可接受——它是**补充臂**、不进论文主表，
#   且正典 `runs/seed{S}/best.pt` 与 42 份概率缓存**都还在**，全部指标可离线重算。
# ⚠ 规则顺序敏感：必须写在上面的 `!runs/**/best.pt` **之后**，后者才被覆盖。
runs/perclass_arm/**/best.pt

# 目录：`products/alldata/perclass_labels/`（逐类标签文件，2026-09-23）。
# 可由 `scripts/run_perclass_arm.py --steps labels` 从 `alldata(readonly)/contract_labels.json`
# **逐位重建**（只是把除第 c 列外全部置 0），故整体排除——同 `graphs_ft/` 一类"可重建的中间产物"。
# ⚠ 用**前缀通配**而非字面量（本仓同形态已踩坑三次，见 AGENTS.md）。
products/**/perclass_labels/**

# 🔴 §37 正典的**微调编码器**：`runs/codebert_ft/<语料>/ss{S}/encoder/`（2026-09-20 补规则）。
# 实测 **2.9 GB**，其中 6 份 `pytorch_model.bin` 各 **498 MB** —— 单文件即超本仓「不提交 >100 MB」硬规则。
# 上面那条 `runs/**/*.pt` **只认 `.pt`**，`.bin`/`tokenizer.json` 全部漏网 ⇒ 补规则前 `git add -A`
# 会一次性把它们纳入（与 `graphs_ft/` 那次是同一类洞，见 AGENTS.md「复现所需的最小集必须入库」）。
# 权重可由 `scripts/finetune_codebert.py` 重建（① 848 s/种子、② 3797 s/种子），故**不入库**。
# 保留的是**自述/审计边车**（几 KB）：`corpus.json` 是「这个编码器属于哪个语料」的硬校验锚点
# （`build_graph_variant.py` / `build_dive_external_set.py` 缺它即硬失败）。
# ⚠ 写法与 `products/**/graphs_ft/**` 同理：`dir/*` 与 `dir/` 都**不能**配合 `!` 白名单。
# 2026-09-21 泛化为 `codebert_ft*`：同一天又新增了两个同前缀目录
#   `runs/codebert_ft_buggy/`（任务2 含 buggy 的编码器）、`runs/codebert_ft_probe/`（epoch 探针），
#   字面量规则**一个都匹配不到** ⇒ 实测这两处各有 **475 MB** 的 `pytorch_model.bin` 会被
#   `git add -A` 纳入（`git add -A --dry-run` 实测 42 个文件 / 958.5 MB）。
#   **同一形态第三次踩坑**（`graphs_ft` → `codebert_ft` → 本轮），故改用通配一次覆盖。
runs/codebert_ft*/**
!runs/codebert_ft*/**/
!runs/codebert_ft*/**/corpus.json
!runs/codebert_ft*/**/config.json
!runs/codebert_ft*/**/special_tokens_map.json

# 🔴 5.3 三条论文基线的**离线中间产物**：`products/alldata/baseline/{mvdhg,egfl,mando}/`
#    （2026-09-22 新增形态）。MVD-HG 的 `sol_source/`+`AST_json/`+`raw/`+`graphs/`，
#    EGFL 的 `seq/`+`feat/`、以及各自的 `w2v.model`。
#    **整体排除**：它们是**离线特征**（可由 `baseline_*_build.py` 重建），不是复现所需的最小集——
#    下游汇总只读 `eval_results/baseline/<name>/seed{S}/test_probs.pt`。
#    ⚠ 按本仓教训用**前缀通配**（同形态已踩坑三次：`graphs_ft`→`codebert_ft`→`graphs_ft_buggy`）。
products/**/baseline/**
!products/**/baseline/**/
!products/**/baseline/**/manifest.json
!products/**/baseline/**/hgt_metadata.json
!products/**/baseline/**/opcodes.json

# 三基线的**模型产物**：与 `runs/**` 同一口径 —— 只入**可离线重算指标的**概率缓存，
# `best.pt` 排除（三个基线 × 3 种子 = 9 个，各约 3–11 MB；指标全部可由 probs 离线重算）。
# ⚠ 规则顺序：`eval_results/baseline/**/*.pt` 必须写在两条 `!` **之前**才有效。
# ⚠ **不要**写成 `eval_results/baseline/**`（那会把 slither 的 `seed{S}_eval.json`
#    与三基线的 results/config/thresholds/log 全部扫掉——它们是要入库的读数本体）。
eval_results/baseline/**/*.pt
!eval_results/baseline/**/test_probs.pt
!eval_results/baseline/**/val_best_probs.pt
!runs/codebert_ft*/**/tokenizer_config.json