# 论文开发手册：M1-M5 实施指南

本文档根据研究点一细化大纲整理，默认 M1、M2、M3、M4、M5 都还没有执行过。也就是说，本文按“零状态”来写输入、输出、格式和验收标准，不预设任何中间产物已经生成。

本文的核心任务不是做单标签漏洞判断，而是：

- 以去重后的唯一合约为样本，做七类漏洞的合约级多标签分类。
- 在图级分类的同时输出节点重要性分数，用于解释和可视化。
- 节点重要性分数是辅助解释结果，不应被写成真实漏洞根因标签。

如果只记住一句话，就记住这句：主任务是图级多标签分类，节点级结果是解释，不是硬定位承诺。

---

## 0. 统一形式化定义与模块分工

### 0.1 问题定义

给定智能合约 $C$，由 Slither 统一解析后构建异构图 $G=(V,E)$。其中节点集 $V$ 以 `CFGNode` 为核心，边集 $E$ 覆盖四类关系：

- `AST_PARENT`
- `CFG_FLOW`
- `DFG_DEP`
- `CALLBACK_RISK`

其中 `CALLBACK_RISK` 只作为保守结构先验，不表示本文已经恢复了真实跨合约调用路径，也不承诺外部合约一定存在回调逻辑。

### 0.2 任务目标

本文在图级多标签标签 $Y_G$ 约束下，学习合约级多标签漏洞分类函数 $f(G)$ 和辅助解释用的节点可疑度分数 $a_v$。其中，分类任务是主监督目标；节点可疑度分数不被视为有真实节点标签监督下的精确定位输出。

### 0.3 损失与输出

图级多标签分类损失定义为：

$$
L_{cls}=L(f(G),Y_G)
$$

其中 $f(G)$ 输出 $K$ 维 logits，$K=7$；训练时使用 `BCEWithLogitsLoss`。

节点可疑度分数定义为：

$$
a_v = \sigma(w^T h_v^{(L)})
$$

并按 $V^*=TopK(a_v,k)$ 输出可疑节点排序。若在 SolidiFI 上做验证，$V^*$ 只能称为合成注入节点覆盖评估，不能外推为真实漏洞根因定位。为便于可解释性展示，可以对 $V^*$ 做局部邻域扩展，得到可视化子图 $G_{view}$；连通性不作为训练阶段的硬约束，只作为推理后的可视化处理方式。

MVD-HG 数据按七种漏洞类别文件夹组织，但同一合约可能出现在多个漏洞类别文件夹中，因此标签不是互斥单标签。本文以去重后的唯一合约为样本，将出现在某漏洞类别文件夹中的合约对应类别置为 1，否则置为 0，得到多热标签向量 $y_G\in\{0,1\}^K$，其中 $K=7$。若后续额外引入正常合约，正常合约表示为全 0 标签向量，并单独说明数据来源与划分。默认损失函数为 `BCEWithLogitsLoss`。

### 0.4 五个模块的角色分工

| 模块 | 在统一目标中的角色 |
| --- | --- |
| M1 | 节点先验打分：基于 Slither 静态规则生成节点先验分数 $s_v$，用于辅助节点特征初始化 |
| M2 | 异构图构建：构建以 `CFGNode` 为节点、`CFG_FLOW`、`DFG_DEP` 和 `CALLBACK_RISK` 为边类型的异构图；其中 `CALLBACK_RISK` 仅以保守规则生成，不解析外部合约 |
| M3 | 节点特征初始化：融合 CodeBERT 节点文本表示、结构特征和先验分数，生成初始节点表示 $h_v^{(0)}$ |
| M4 | RGCN 消息传递：通过关系图卷积学习节点表示 $h_v^{(L)}$，并输出节点可疑度分数 $a_v$ |
| M5 | 训练与推断：以合约级多标签分类损失为主进行训练，推理阶段输出每类漏洞概率和节点可疑度排序 |

---

## 1. 统一任务定义与数据口径

### 1.1 任务定义

给定智能合约源码，先构建异构程序图，再在图级完成七类漏洞的多标签分类。图上的节点是 CFGNode，边至少包括以下四类：

- AST_PARENT
- CFG_FLOW
- DFG_DEP
- CALLBACK_RISK

其中 CALLBACK_RISK 只是一种保守结构先验，不表示已经恢复了真实跨合约调用路径，也不意味着外部合约一定存在回调逻辑。

### 1.2 标签定义

本文使用七类漏洞标签：

- access_control
- arithmetic
- dos
- front_running
- reentrancy
- time_manipulation
- uncheck_return

数据集按唯一合约去重后，一个合约可以同时属于多个类别，因此标签应表示成多热向量 $y_G \in \{0,1\}^7$，而不是互斥单标签。

### 1.3 节点重要性定义

节点重要性分数记为 $a_v$，作用是给图级分类提供可解释排序和局部可视化。它的用途是：

- 排序关键节点
- 做局部子图展示
- 观察模型是否学到了和漏洞相关的结构

它不等同于真实漏洞根因节点集合。

### 1.4 训练目标

主监督目标是图级多标签分类，默认使用 BCEWithLogitsLoss。节点重要性分数不是独立监督目标，也不应被强行写成单独的真值回归问题。

### 1.5 输出产物

整个流程最终应该至少得到这些产物：

1. M1 的节点先验文件
2. M2 的异构图 JSON 文件
3. M2 转换后的 PyG 文件
4. M3 的节点初始表示
5. M4 的节点重要性分数和图向量
6. M5 的训练日志、分类结果和解释结果

---

## 2. M1 开发手册：静态先验打分

### 2.1 M1 的目标

M1 的目标是给每个节点生成一个静态先验分数 $s_v$，帮助后续模块知道哪里值得重点关注。它不是最终分类器，也不是节点级真值定位器。

### 2.2 M1 的输入

M1 可以直接读取两类输入：

1. 旧的 Slither 解析 JSON
2. M2 产出的 `_hetero.json`

在当前仓库里，`anchor_detectors.py` 已经支持 `GraphContext.from_any_json()`，所以实现上可以兼容两种输入。对新手来说，建议优先让 M1 能读当前的 `_hetero.json`，这样可以和后续模块共享同一份图数据。

### 2.3 M1 的输出

建议输出两层结果：

1. 节点级布尔命中表 `node_flags`
2. 节点级先验分数表 `node_scores`

如果你希望用于调试，还可以额外保存一个互斥标签版本，但论文主线里更重要的是原始布尔命中和分数，而不是把七类规则压成单标签。

建议格式如下：

```json
{
  "node_flags": {
    "9": {
      "reentrancy": true,
      "arithmetic": false,
      "access_control": true,
      "uncheck_return": true,
      "dos": false,
      "front_running": false,
      "time_manipulation": false
    }
  },
  "node_scores": {
    "9": 0.4285714286
  }
}
```

### 2.4 M1 的核心规则

七类规则与大纲中的定义保持一致：

1. reentrancy：低级 call / send / transfer / external call 相关节点，以及调用后的状态写入。
2. arithmetic：unchecked 块中的算术运算。
3. access_control：危险操作和权限变量共现。
4. uncheck_return：send/call 返回值没有被消费。
5. dos：循环体内外部调用，且沿 CFG 做有限深度邻域观察。
6. front_running：竞态状态变量写入节点。
7. time_manipulation：block.timestamp 或 now 读取。

另外，论文大纲里还提到一个辅助性的 external_callback / CALLBACK_RISK 语义。它不是独立漏洞类别，而是给 reentrancy、部分 dos 和 uncheck_return 提供保守先验。这个信号更适合被当成结构先验或额外边信息使用，而不是和七类主标签混在一起。

### 2.5 M1 的详细步骤

#### 步骤 1：读取图数据或 Slither JSON

输入：

- `_hetero.json` 或 Slither JSON

处理：

- 读取 `meta`
- 读取节点表
- 读取边表

输出：

- 原始 JSON 对象

验收：

- 能拿到节点数、边数和源文件信息

#### 步骤 2：构造 GraphContext

输入：

- 原始 JSON

处理：

- 把节点转成 `node_id -> node_dict`
- 只保留 CFG_FLOW 邻接
- 预计算返回值是否被消费
- 载入状态变量白名单

输出：

- GraphContext

验收：

- `ctx.nodes` 能查到表达式文本
- `ctx.adj_cfg` 能查到后继节点

#### 步骤 3：逐节点计算布尔规则

输入：

- `node_id`
- GraphContext

处理：

- 依次判断七类漏洞模式
- 记录每类是否命中

输出：

- 七个布尔值

验收：

- 字典里必须始终有七个键
- 空表达式节点不能报错

#### 步骤 4：把布尔命中转成先验分数

输入：

- 七类布尔命中结果

处理：

- 先按命中数量给初始分数
- 对 DFG 反向邻居做一跳传播
- 对全图分数做 min-max 归一化

输出：

- $s_v \in [0,1]$

验收：

- 没有命中时分数接近 0
- 多类命中时分数更高

#### 步骤 5：批量导出

输入：

- 全图 GraphContext

处理：

- 遍历所有节点
- 生成 `node_flags` 和 `node_scores`

输出：

- M1 JSON 文件

验收：

- 每个节点都有分数
- 能对样例图做人工抽查

### 2.6 M1 的新手检查清单

建议先拿样例图做这几个检查：

1. `LastMsg.Time = now` 是否命中 time_manipulation。
2. `msg.sender.call.value(_am)()` 是否命中 reentrancy。
3. `MinSum = _val` 是否被误判为前跑漏洞。
4. `revert()()` 这种节点是否被错误当成返回值检查。

如果这四项不稳定，先不要继续做后面的 M3-M5。

---

## 3. M2 开发手册：CFG 中心异构图构建

### 3.1 M2 的目标

M2 的目标是把编译和解析产物整理成统一的图交换格式。它不是训练模型，而是保证后续模块都能稳定消费同一份图数据。

### 3.2 M2 的输入

M2 至少需要四类输入：

1. AST JSON
2. CFG dot 文件或等价 CFG 中间文件
3. DFG 文本输出
4. Solidity 源码根目录

### 3.3 M2 的输出

建议输出两份文件：

1. `_hetero.json`
2. `_pyg.pt`

其中 `_hetero.json` 是论文和模块之间的主交换格式，`_pyg.pt` 是给后续 PyG 代码直接读取用的桥接文件。

### 3.4 `_hetero.json` 的标准结构

建议保持如下顶层结构：

```json
{
  "meta": {
    "source": "...",
    "source_path": "...",
    "cfg_node_count": 0,
    "cfg_edge_count": 0,
    "ast_edge_count": 0,
    "ast_same_cfg_edge_count": 0,
    "ast_unmapped_edge_count": 0,
    "dfg_edge_count": 0,
    "dfg_parse_errors": 0,
    "expr_span_total": 0,
    "expr_span_missing": 0
  },
  "nodes": [...],
  "edges": {
    "CFG_FLOW": [...],
    "AST_PARENT": [...],
    "DFG_DEP": [...],
    "CALLBACK_RISK": [...]
  }
}
```

说明：当前仓库里还可能保留 `AST_PARENT_SAME` 作为实现细节，用于处理多个 AST 节点映射到同一个 CFG 节点的情况。论文正文里可以把它并入 AST 映射的工程细节，主结构仍以四类边为准。

### 3.5 节点字段建议

每个节点至少建议包含：

```json
{
  "id": 1,
  "cfg_node_type": "EXPRESSION",
  "contract": "LogFile",
  "function": "AddMessage",
  "expression": "LastMsg.Sender = _adr",
  "span": [1553, 1574],
  "line_start": 90,
  "line_end": 90
}
```

如果某些字段回填不出来，也不要强行中断流程，但必须在 `meta` 里体现缺失情况。

### 3.6 M2 的详细步骤

#### 步骤 1：读取 AST、CFG、DFG 和源码

输入：

- AST JSON
- CFG dot
- DFG 文本
- 源码目录

处理：

- 读取 AST 根节点和合约信息
- 解析 CFG 节点类型和表达式
- 解析 DFG 变量依赖
- 回填源码路径和源码文本

输出：

- 原始编译产物集合

验收：

- 同一样本能同时找到 AST、CFG、DFG 三类输入
- 源码路径存在且可读

#### 步骤 2：构建 CFG 中心节点表

输入：

- CFG 节点及局部边

处理：

- 为每个 CFG 节点分配稳定全局 id
- 记录 `cfg_node_type`
- 记录 `contract`、`function`、`expression`
- 尽量回填 `span`、`line_start`、`line_end`

输出：

- `nodes` 列表

验收：

- 每个节点都有整数 id
- 节点缺少表达式时也能继续生成图

#### 步骤 3：生成 CFG_FLOW 边

输入：

- CFG 局部边

处理：

- 把局部节点编号映射到全局 id
- 只保留有效的控制流后继关系

输出：

- `CFG_FLOW` 边集合

验收：

- `meta.cfg_edge_count` 和 `CFG_FLOW` 数量一致

#### 步骤 4：映射 AST 边

输入：

- AST 节点和 AST 父子边

处理：

- 把 AST 节点尽量映射到对应的 CFG 节点
- 无法映射的边单独计数
- 同一 CFG 节点上的重复 AST 映射保留为实现细节

输出：

- `AST_PARENT`，以及必要时的 `AST_PARENT_SAME`

验收：

- AST 映射失败数量可见，而不是静默丢失

#### 步骤 5：生成 DFG_DEP 边

输入：

- DFG 文本解析结果

处理：

- 解析变量定义与使用关系
- 将依赖关系映射到 CFG 节点对

输出：

- `DFG_DEP` 边集合

验收：

- `meta.dfg_edge_count` 和 `DFG_DEP` 数量一致

#### 步骤 6：生成 CALLBACK_RISK 边

输入：

- 外部调用节点
- 本合约可重入入口节点

处理：

- 识别 call / send / transfer / 低级调用节点
- 识别 public / external / receive / fallback 入口
- 仅在保守条件满足时建立边
- 限制单个外部调用节点的连接数，避免过度平滑

输出：

- `CALLBACK_RISK` 边集合

验收：

- 不是所有 public/external 入口都被无条件连接
- 这条边是保守风险提示，不是跨合约真实调用图

#### 步骤 7：序列化并转换为 PyG

输入：

- `nodes`
- `edges`
- `meta`

处理：

- 写出 `_hetero.json`
- 调用 `convert_hetero_json_to_pyg.py` 生成 `_pyg.pt`

输出：

- 两份可复用的图产物

验收：

- `convert_hetero_json_to_pyg.py` 能正确读出 `cfg_node.x`
- `node_id`、`contract`、`function`、`expression` 被保留用于追踪

### 3.7 M2 的新手检查清单

1. `_hetero.json` 的 `nodes` 数量是否和 `meta.cfg_node_count` 一致。
2. 四类边是否都存在，且边数不是全 0。
3. `span` 和 `line_start/line_end` 是否能在样例图上回填出来。
4. `_pyg.pt` 是否能被后续模型直接加载。

---

## 4. M3 开发手册：节点特征初始化

### 4.1 M3 的目标

M3 的目标是把每个节点变成神经网络可以处理的向量。它不是规则检测，而是把文本语义、结构特征和静态先验组合成初始节点表示。

### 4.2 M3 的输入

M3 需要四类输入：

1. 异构图 `G_hetero`
2. 节点源码片段或函数级源码文本
3. 节点 span / line 信息
4. M1 的先验分数 `s_v`

### 4.3 M3 的输出

默认输出每个节点的初始表示 $h_v^{(0)}$。按照大纲的统一训练设定，后续消息传递层的默认隐藏维度是 128，因此这里建议也对齐到 128 维，避免前后模块维度不一致。

### 4.4 M3 的三条通道

#### 通道一：节点类型嵌入

把节点语义角色映射成可学习向量。大纲里建议的节点角色至少包括：

- ENTRY
- CONDITION
- ASSIGNMENT
- EXT_CALL
- INT_CALL
- STATE_WRITE
- STATE_READ
- RETURN
- OTHER

这部分是结构语义，不是分类标签。

#### 通道二：函数级 CodeBERT 表示

不要按每个节点单独切很短的窗口反复编码，而是以函数为单位提取源码，再用 CodeBERT 编码得到函数级全局语义表示。然后让同一函数里的所有 CFGNode 共享这份表示。

这个设计的好处是：

- 避免相邻节点文本高度重叠造成的重复计算
- 降低显存和时间开销
- 保留函数级上下文，而不是只看单行表达式

主方案里 CodeBERT 参数保持冻结，不做微调。

#### 通道三：节点结构特征

结构特征是静态可提取的，不依赖真实标签。建议至少包括：

- 函数可见性 one-hot
- 是否包含外部调用
- 是否包含转账操作
- 是否写状态变量
- 是否读状态变量
- 是否为条件判断节点
- 是否包含算术操作
- 是否使用 block.timestamp
- 是否使用 msg.sender 或 tx.origin
- 是否涉及调用返回值
- 是否位于循环体内
- M1 输出的节点先验分数 $s_v$

如果你要更贴近大纲，还可以把外部调用方式细分成：call、send、transfer、低级调用、其他，以及返回值是否被 require / if / assert 消费、当前节点后是否存在状态写入、节点是否属于入口函数等。

### 4.5 M3 的详细步骤

#### 步骤 1：准备函数级文本

输入：

- 节点表达式
- 所属函数源码

处理：

- 提取当前函数的源码文本
- 如果超过长度限制，优先保留主干逻辑
- 让同一函数中的节点共享函数级编码

输出：

- 函数级文本窗口

验收：

- 节点文本和源码一致
- 不因为短窗口切碎逻辑语义

#### 步骤 2：生成节点类型嵌入

输入：

- 节点类型

处理：

- 把类型编码成可学习向量

输出：

- 类型嵌入向量

验收：

- 同一类型节点的初始类型向量一致

#### 步骤 3：生成函数级 CodeBERT 表示

输入：

- 函数级文本

处理：

- 送入冻结的 CodeBERT
- 取池化后的语义表示

输出：

- 函数语义向量

验收：

- 文本不同，语义向量应有明显区别

#### 步骤 4：生成结构特征

输入：

- 节点属性

处理：

- 把可见性、调用方式、写读状态、循环位置等属性拼成结构特征向量

输出：

- 结构特征向量

验收：

- 缺失属性时必须有默认值

#### 步骤 5：先验 Dropout

输入：

- 节点先验分数 $s_v$

处理：

- 训练时以概率 0.2 将当前样本所有节点的 $s_v$ 置零
- 验证和推理时保留原值

输出：

- 训练态先验特征

验收：

- 训练和推理行为不同，但输入格式一致

#### 步骤 6：拼接并映射到 $h_v^{(0)}$

输入：

- CodeBERT 表示
- 类型嵌入
- 结构特征
- 先验分数

处理：

- 拼接后通过 MLP 映射到固定维度

输出：

- 节点初始表示 $h_v^{(0)}$

验收：

- 每个节点都输出相同维度向量
- 训练中允许结构特征 Dropout

### 4.6 M3 的新手检查清单

1. 随便取一个节点，看输入文本是否和源码一致。
2. 看 $h_v^{(0)}$ 维度是否恒定。
3. 看同一函数内的节点是否共享 CodeBERT 语义。
4. 看缺 span 的节点是否还能跑完。

---

## 5. M4 开发手册：RGCN 消息传递与节点重要性学习

### 5.1 M4 的目标

M4 的目标是让节点之间的信息沿异构边传播，并从传播后的表示中学习节点重要性分数 $a_v$ 和图级表示 $h_G$。

### 5.2 M4 的输入

M4 需要：

1. 异构图边索引
2. $h_v^{(0)}$
3. 边类型信息

### 5.3 M4 的输出

M4 输出三样东西：

1. 节点表示 $h_v^{(L)}$
2. 节点重要性分数 $a_v$
3. 图级表示 $h_G$

### 5.4 M4 的默认超参数

按大纲的主方案，建议先固定成下面这组配置：

- 消息传递层数 $L = 2$
- 隐藏维度 $d = 128$
- 关系数：4 类边
- Dropout：0.3
- DropEdge：0.1
- 权重衰减：$10^{-4}$

### 5.5 M4 的详细步骤

#### 步骤 1：确定关系类型

输入：

- CFG_FLOW
- AST_PARENT
- DFG_DEP
- CALLBACK_RISK

处理：

- 每种边类型单独建消息通道

输出：

- 关系集合

验收：

- 所有边类型都能被识别

#### 步骤 2：做关系区分的消息传递

输入：

- $h_v^{(0)}$
- 边索引

处理：

- 用 RGCN 按关系类型做两层传播

输出：

- $h_v^{(L)}$

验收：

- 节点向量维度保持固定

#### 步骤 3：计算节点重要性分数

输入：

- $h_v^{(L)}$

处理：

- 用线性层或小 MLP 输出 `logit_v`
- 再经 sigmoid 得到 $a_v$

输出：

- 节点重要性分数

验收：

- 每个节点有一个标量分数

#### 步骤 4：做图级 Readout

输入：

- $h_v^{(L)}$
- $a_v$

处理：

- 按重要性加权汇聚得到图表示 $h_G$

输出：

- 图向量 $h_G$

验收：

- 每个合约都能得到一个固定维度图向量

#### 步骤 5：推理时输出 TopK 节点

输入：

- $a_v$

处理：

- 取 TopK 高分节点作为解释节点
- 必要时做一跳邻域扩展

输出：

- 可视化子图

验收：

- 高分节点可以用于人工检查

### 5.6 M4 的新手检查清单

1. 看关系门控或关系权重是不是会变化。
2. 看掩码能不能把明显无关节点压低。
3. 看两层传播后节点向量维度是否稳定。
4. 看图向量维度是否固定。

---

## 6. M5 开发手册：端到端训练与评估

### 6.1 M5 的目标

M5 的目标是把前面的模块真正训练起来，并证明它在图级多标签分类上不是只会背训练集。

### 6.2 M5 的输入

M5 至少需要：

1. 图级表示 $h_G$
2. 节点重要性 $a_v$
3. 图级标签 $Y_G$
4. 节点先验分数 $s_v$

### 6.3 M5 的输出

1. 合约级漏洞分类预测
2. 最终节点排序
3. 训练日志和评估指标

### 6.4 损失函数

默认主损失是图级多标签分类损失：

$$
L_{total} = L_{cls}
$$

其中 $L_{cls}$ 使用 BCEWithLogitsLoss。

如果类别不平衡明显，可以在训练集上为每类设置 `pos_weight`，但不要把节点重要性强行变成独立的真值监督项。

### 6.5 优化和训练配置

建议先采用下面这套默认配置：

- 优化器：AdamW
- 学习率：$10^{-4}$
- 权重衰减：$10^{-4}$
- 梯度裁剪：1.0
- batch size：32
- 最大 epoch：80

如果验证集 macro-F1 明显震荡，可以把学习率降到 $5 \times 10^{-5}$。

### 6.6 M5 的训练流程

#### 步骤 1：准备标签文件

输入：

- `contract_labels.json`
- `solidifi_labels.json`

处理：

- 把每个合约映射成七维多热标签

输出：

- 图级标签 $Y_G$

验收：

- 每个图都能对应到标签

#### 步骤 2：Phase 1 联合训练

输入：

- 全部训练图

处理：

- 训练分类头和图神经网络主体
- 让节点重要性在分类任务中间接学习

输出：

- 初步收敛的模型参数

验收：

- 验证集 macro-F1 能随 epoch 上升并趋于稳定

#### 步骤 3：温度和正则策略

输入：

- 当前 epoch

处理：

- 如果使用 mask 或稀疏化机制，可以按 epoch 调整温度或稀疏强度

输出：

- 更稳定的节点选择

验收：

- 稀疏度不会长期随机波动

#### 步骤 4：验证与早停

输入：

- 验证集预测

处理：

- 计算 macro-F1、micro-F1、每类 F1、mAP 或 macroPR-AUC
- 以验证集 macro-F1 作为主模型选择指标
- 连续 5 个 epoch 不提升时早停

输出：

- 最佳 checkpoint

验收：

- 至少保留一份验证集最优模型

#### 步骤 5：推理输出

输入：

- 训练好的模型

处理：

- 输出七类漏洞概率
- 输出节点重要性排序

输出：

- 合约级预测 + 节点排序

验收：

- 预测结果可复现

### 6.7 M5 的评估口径

建议把评估分成两层：

1. 真实合约上的合约级多标签分类评估
2. 合成注入漏洞上的节点排序评估

#### 真实合约评估

使用 MVD-HG 和 SmartBugs 做合约级评估，报告至少包括：

- macro-F1
- micro-F1
- 每类 Precision
- 每类 Recall
- 每类 F1
- mAP 或 macroPR-AUC

阈值搜索只在验证集上做，测试集不能参与阈值选择。主实验建议至少跑 3 个随机种子，并报告均值和标准差。

#### 节点排序评估

用 SolidiFI 做合成注入漏洞上的节点排序评估，报告：

- 模型分数 $a_v$
- 静态先验分数 $s_v$
- 梯度显著性分数 $g_v$

指标建议包括 Precision@k、Recall@k、IoU。需要诚实说明：SolidiFI 的结果主要反映合成注入场景下的覆盖情况，不能被表述为真实漏洞根因定位能力。

### 6.8 M5 的新手检查清单

1. 训练时有没有 NaN。
2. 验证集 F1 有没有提升。
3. 节点排序是不是比静态先验更稳定。
4. 训练集和验证集差距是不是过大。

---

## 7. 建议的实际开发顺序

如果你现在是从零开始，建议按下面顺序执行：

1. 先把 AST / CFG / DFG 产物稳定下来，完成 M2。
2. 再让 M1 能读当前的图数据，输出 `node_flags` 和 `node_scores`。
3. 接着做 M3，把函数级文本、结构特征和先验分数拼成初始节点表示。
4. 然后做 M4，用 RGCN 学节点重要性和图级表示。
5. 最后做 M5，完成端到端训练、验证和解释输出。

这么排的原因很简单：

- 数据格式不稳，后面全都会错。
- M1 不稳定，先验特征就不能可靠进入 M3。
- M3 不稳定，M4 学到的表示没有基础。
- M4 不稳定，M5 的解释输出就不可信。

---

## 8. 最容易犯的错误

1. 把静态规则命中当成最终标签。
2. 把节点重要性分数当成真实根因定位结果。
3. 把 CALLBACK_RISK 当成真实跨合约调用图。
4. 把训练和推理的 Dropout、温度、Mask 行为混为一谈。
5. 不做唯一合约去重，导致同一合约跨划分泄漏。
6. 只跑模型，不做输入校验。

---

## 9. 最低可用交付标准

如果你要先交一个能跑的版本，至少要满足这些条件：

1. M1 能对样例图输出七类 flags 和先验分数。
2. M2 能生成 `_hetero.json` 和 `_pyg.pt`。
3. M3 能把每个节点变成固定维度的初始向量。
4. M4 能输出节点重要性和图级表示。
5. M5 能完成一次完整训练并产生日志和预测结果。

只要这五件事能稳定做出来，后面的消融、外部测试和可解释性展示才有可靠基础。
