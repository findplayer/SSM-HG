# 消融三方并列 + DIVE 外部测试：结果与分析

> 大纲依据：`改II` 5.2 层次一（同分布 + 跨数据集两设定）、5.5.1（DIVE 五项分析）。
> 决议：`decisions.md` §38。程序化产物：`eval_results/dive/comparison.{json,md}`（本文件的表 1–4 由
> `scripts/collect_dive_comparison.py` **程序复算**，不手抄）。

---

## 0. 一句话结论

| 问题 | 答案 |
| --- | --- |
| ①②内测里哪个消融**有作用**？ | 只有 **`cb_frozen`（冻结编码器）** 效应量大到 n=3 就能看见（① −0.276、② −0.041，均 ★）。其余 20 项**都判不了**——见 §3.1。 |
| 哪个消融**没作用**？ | 严格说：**没有一项能被判定为"无作用"**。n=3 判不了 ±0.05 量级的效应（`decisions.md` §26.7/§27.5）；`cb_unlimited` 在①②上的 Δ **恰为 0**，但那是**没有功效**而非没有效应（§3.3）。 |
| DIVE 上还能检出漏洞吗？ | **F1 近乎归零**（① 模型 0.015、② 模型 0.061）。根因是**类别先验错配**，不是代码故障（§4.2）。 |
| 换个数据集，模型的"排序能力"保住多少？ | 这是**最重要的发现**：把 mAP 减去随机基线后，同分布 **+0.693** → 跨数据集 **+0.052**，**只保住 7.4%**（§4.3）。 |
| 消融结论在 DIVE 上还成立吗？ | ① 模型在 DIVE 上**所有臂都挤在 ±0.013 内**，什么都分不出来；② 模型有几个正号但 std 极大 ⇒ **消融结论不可外推**（§4.4）。 |

---

## 1. 做了什么

1. **消融全部重跑**（正典改为微调 CodeBERT，`decisions.md` §37）：① 21 臂 × 3 种子、② 21 臂 × 3 种子 = **126 run，0 失败**。② 由原设计 5 臂扩到 21 臂，与 ① **逐臂对齐**。
2. **DIVE 外部测试**：把训练好的模型**原样**搬到 DIVE 抽样集上推理。协议**不重抽**——沿用已冻结的 `seed=0 / n=900`；DIVE 实测解析后 **890 图**。
3. **三方并列表**：① 内测 / ② 内测 / DIVE(①模型) / DIVE(②模型)。

**三条纪律**（`AGENTS.md` + 大纲 5.1）：

- DIVE **只读**，且**不参与任何模型选择**（训练/验证/早停/阈值/架构都不看 DIVE）；
- 外部测试的阈值**只读源语料验证集**的搜索结果（`thresholds.json`），**绝不在 DIVE 上重搜**；
- 编码器矩阵：每个语料的模型配**它自己**微调的编码器。图结构只建一份，特征建三套。

---

## 2. 三方主表

### 表 1：micro-F1@val_thr（3 种子 mean±std）

| 臂 | 变量 | ① 主库内测 | ② 增强集内测 | DIVE(①模型) | DIVE(②模型) |
| --- | --- | --- | --- | --- | --- |
| **正典** | 微调 CodeBERT + 全部组件 | **0.7297**±0.0675 | **0.9913**±0.0076 | **0.0152**±0.0167 | **0.0614**±0.0350 |
| ast_parent | 去 AST_PARENT（关系 1+2） | 0.6889±0.0801 | 0.9859±0.0095 | 0.0074±0.0051 | 0.0378±0.0067 |
| callback_risk | 去 CALLBACK_RISK（关系 4） | 0.6960±0.0271 | 0.9891±0.0094 | 0.0170±0.0162 | 0.0630±0.0340 |
| cb_frozen | **冻结** CodeBERT（正典=微调） | 0.4537±0.0871 | 0.9508±0.0267 | 0.0106±0.0072 | 0.0323±0.0168 |
| cb_func_only | 去节点级 CodeBERT | 0.7429±0.0394 | 0.9847±0.0137 | 0.0046±0.0009 | 0.0897±0.0547 |
| cb_node_only | 去函数级 CodeBERT | 0.7500±0.0634 | 0.9879±0.0117 | 0.0095±0.0047 | 0.0680±0.0415 |
| cb_rev | 加 CALLBACK_RISK_REV（5→6 关系） | 0.7536±0.0322 | 0.9902±0.0085 | 0.0083±0.0057 | 0.1279±0.1621 |
| cb_unlimited | CALLBACK_RISK 出边不限上限 | 0.7297±0.0675 | 0.9923±0.0069 | 0.0146±0.0157 | 0.0614±0.0270 |
| cfg_flow | 去 CFG_FLOW（关系 0） | 0.6829±0.1274 | 0.9879±0.0118 | 0.0034±0.0027 | 0.0484±0.0090 |
| dfg_dep | 去 DFG_DEP（关系 3） | 0.7149±0.0843 | 0.9838±0.0059 | 0.0052±0.0037 | 0.1130±0.0403 |
| dropedge02 | DropEdge 概率 0.2 | 0.6422±0.0872 | 0.9881±0.0104 | 0.0061±0.0037 | 0.0751±0.0131 |
| feat_base | 结构特征分组 base | 0.6758±0.0080 | 0.9912±0.0084 | 0.0149±0.0127 | 0.0702±0.0213 |
| feat_base_sem | 结构特征分组 base+sem | 0.7048±0.0670 | 0.9913±0.0076 | 0.0143±0.0111 | 0.0654±0.0271 |
| hid256 | 隐藏维度 256（基线 128） | 0.7543±0.0199 | 0.9880±0.0114 | 0.0040±0.0019 | 0.0748±0.0569 |
| layers1 | RGCN 层数 1（基线 2） | 0.7760±0.0372 | 0.9912±0.0071 | 0.0040±0.0023 | 0.0427±0.0369 |
| layers3 | RGCN 层数 3（基线 2） | 0.7523±0.0502 | 0.9793±0.0187 | 0.0052±0.0011 | 0.2005±0.1160 |
| meanpool | meanpool 替换 a_v 加权 readout | 0.7551±0.0304 | 0.9816±0.0137 | 0.0046±0.0028 | 0.0512±0.0029 |
| no_lvar | 关闭 L_var | 0.7342±0.0598 | 0.9923±0.0069 | 0.0146±0.0157 | 0.0617±0.0344 |
| no_prior_drop | 关闭先验 Dropout | 0.6962±0.0531 | 0.9892±0.0098 | 0.0161±0.0147 | 0.0627±0.0333 |
| numbases1 | num_bases=1 | 0.7462±0.0130 | 0.9880±0.0042 | 0.0034±0.0019 | 0.0567±0.0441 |
| numbases3 | num_bases=3 | 0.7797±0.0354 | 0.9838±0.0147 | 0.0025±0.0005 | 0.0754±0.0418 |
| numbases4 | num_bases=4 | 0.8077±0.0339 | 0.9798±0.0206 | 0.0046±0.0024 | 0.0678±0.0508 |

> 🔴 **禁止跨列比较绝对值**（`decisions.md` §23）：四列是**四套不同的评测条件**。
> 可比的只有**同列内的 Δ**（相对该列自己的正典）。

### 表 2：Δmicro-F1@val_thr（★ = |Δ| > 该列正典的种子间 std）

| 臂 | Δ① | Δ② | ΔDIVE(①) | ΔDIVE(②) |
| --- | --- | --- | --- | --- |
| cb_frozen | **−0.2760★** | **−0.0405★** | −0.0046 | −0.0291 |
| dropedge02 | −0.0875★ | −0.0032 | −0.0090 | +0.0137 |
| numbases4 | **+0.0780★** | −0.0114★ | −0.0105 | +0.0064 |
| feat_base | −0.0539 | −0.0000 | −0.0002 | +0.0087 |
| numbases3 | +0.0500 | −0.0075 | −0.0127 | +0.0139 |
| cfg_flow | −0.0468 | −0.0034 | −0.0118 | −0.0130 |
| layers1 | +0.0463 | −0.0001 | −0.0112 | −0.0188 |
| ast_parent | −0.0408 | −0.0054 | −0.0078 | −0.0236 |
| callback_risk | −0.0337 | −0.0021 | +0.0018 | +0.0016 |
| no_prior_drop | −0.0335 | −0.0021 | +0.0009 | +0.0013 |
| feat_base_sem | −0.0249 | +0.0000 | −0.0008 | +0.0040 |
| hid256 | +0.0246 | −0.0033 | −0.0112 | +0.0133 |
| cb_rev | +0.0239 | −0.0010 | −0.0069 | **+0.0665★** |
| layers3 | +0.0226 | −0.0120★ | −0.0099 | **+0.1391★** |
| meanpool | +0.0254 | −0.0097★ | −0.0106 | −0.0102 |
| cb_node_only | +0.0203 | −0.0034 | −0.0057 | +0.0066 |
| numbases1 | +0.0165 | −0.0033 | −0.0118 | −0.0048 |
| cb_func_only | +0.0132 | −0.0066 | −0.0105 | +0.0283 |
| no_lvar | +0.0045 | +0.0011 | −0.0006 | +0.0003 |
| cb_unlimited | **+0.0000** | +0.0011 | −0.0006 | −0.0001 |
| dfg_dep | −0.0148 | −0.0075 | −0.0099 | **+0.0516★** |

---

## 3. ①② 内测的消融分析（通俗版）

### 3.1 一眼看去"变好"的项，多半是噪声

① 上有 11 项 Δ 为正（`numbases4` +0.078、`numbases3` +0.050、`layers1` +0.046…）。
**但这些都不能当作"该组件有害"的证据**，原因有三：

1. **正典的种子间 std 是 ±0.0675** —— 比绝大多数 Δ 都大；
2. **n=3 判不了 ±0.05 量级的效应**（`decisions.md` §26.7/§27.5 的两条硬规矩，本仓已为此栽过两次）；
3. 而**主实验本身就不是逐位可复现的**：同一配置重跑，Δmicro@0.5 实测 0.0000 / 0.0135 / 0.0223（重跑抖动 ≈0.012，约为种子间 std 的 40%，`decisions.md` §36.4，根因是 CUDA 归约顺序非确定性）。

★ 号只标"|Δ| 超过正典的种子间 std"，意思是**值得复核**，**不是**"效应已确认"。

### 3.2 唯一一个大到不用统计就能看见的：`cb_frozen`

| | ① | ② |
| --- | --- | --- |
| Δmicro@val_thr | **−0.276** | **−0.041** |
| Δmicro@0.5 | −0.298 | −0.057 |

**通俗说**：把编码器从"微调过"换成"原装 CodeBERT"，模型性能掉掉掉——① 上掉了近 28 个点，② 上掉 4 个点。
这与 n=9 同配对复核（`decisions.md` §36）一致：test 侧 8/8 指标显著，`micro@0.5` +0.2466（t=+11.92），9/9 配对全为正。
**这是本论文最结实的一条消融结论**：瓶颈在**输入表征质量**，不在图结构。

### 3.3 `cb_unlimited`：Δ 恰为 0，但**不能**读作"这个组件没用"

`cb_unlimited` 在 ① 上六个指标与正典**逐位相同**。我逐层查到根（`decisions.md` §38.4）：

| 检查 | 结果 |
| --- | --- |
| 变体的 `_pyg.pt` | 20/590 图边集确实不同（且只增不删） |
| 其中在训练池内的 | **只有 9 张**（11 张是 `buggy_*`，已被剔除） |
| 两次训练的 `best.pt` md5 / epoch-0 loss | **都不同**（0.960**3248** vs 0.960**2375**）→ 确实重训了 |
| `test_probs.pt` 的差异 | maxΔ 0.012–0.051 → 概率确实动了 |
| **预测矩阵翻转数** | **0 / 322** → 差异全落在阈值带内，一个决策都没推动 |

**通俗说**：这个消融确实改了图、也确实重训了模型，但改动太"轻"（① 只有 9/453 张图受影响），
而 ① 的测试集只有 46 个合约、322 个标签对，这点扰动推不动任何一个判断。
② 侧同理（199/1774 图改动，但 ② 已在天花板 0.9901，`Δmicro@0.5 = 0.0000`）。

⇒ 论文里这一项必须写成「**本实验未能检验该问题（无功效）**」，
**不得**写成"CALLBACK_RISK 上限不重要"。它本就还叠加着"稀释"缺陷（① 干预面仅 3.4%）。

### 3.4 ② 为什么看不出任何东西

② 上**除 `cb_frozen`（−0.041）外，其余 20 项 Δ 全部落在 ±0.012 内**（实测范围 −0.0114 ~ +0.0011）。
**通俗说**：② 的 micro-F1 已经是 0.99，天花板效应——**没有上升空间，也没有下降空间**。
所以 ② 这一组的作用是"证明模型在训练信号充足时能到顶"，
**不能**用来论证"某个组件重要或不重要"；唯一例外仍是编码器（§3.2）。

### 3.5 一个被反复观察到的形态：拿掉组件，排序变好、F1 变差

① 上 ΔmAP 与 Δmicro 常常反号（`ablation_results.md` §6 有完整计数与复算口径）。
**通俗说**：去掉某个输入后，模型更"敢"给稀有的正类打高分（排序指标 mAP 上去），
但整体阈值化的 F1 反而下来。这不是自相矛盾，是**用召回换排序**。
⚠ 这个计数在文档里**已写错过两次**（原文如此），引用前必须按 `ablation_results.md` §6 的口径复算。

---

## 4. DIVE 外部测试分析（重点）

### 4.1 F1 为什么会崩成这样

| | ① 内测 | DIVE |
| --- | --- | --- |
| 正典 micro-F1@val_thr | 0.7297 | **0.0152** |
| 正典 micro-F1@0.5 | 0.7110 | **0.0282** |

一眼看是"模型完全废了"。但**这不是故障**（§4.2），而是**类别先验错配**：

### 表 4：DIVE 类别先验 vs 训练语料（① 模型）

| 类 | 训练语料正样本率 | DIVE 正样本率 | 漂移 |
| --- | --- | --- | --- |
| access_control | 0.030 | **0.758** | **+0.728（25 倍）** |
| reentrancy | 0.058 | 0.525 | +0.467（9 倍） |
| arithmetic | 0.028 | 0.421 | +0.394（15 倍） |
| time_manipulation | 0.006 | 0.260 | +0.254（43 倍） |
| uncheck | 0.097 | 0.274 | +0.178 |
| dos | 0.011 | 0.152 | +0.141 |
| front_running | 0.006 | 0.034 | +0.028 |

**通俗说**：模型是在一个"**几乎什么都没有**"的世界里长大的——① 的池子里 453 个合约有 **326 个是七类全零**（正常合约），
所以它学到的是"先默认没有问题"。把它扔到 DIVE——那里 **76% 的合约都有访问控制问题**——它还是按老习惯几乎什么都不报。

实测证据：

- 模型输出的概率**中位数只有 0.097**，而 DIVE 的真实正样本率是 **34.6%**；
- 在 @0.5 下它只判出 **0.95%** 的标签对为正；把阈值压到 0.1 才判出 **49.7%**（真值 34.6%）——**概率里是有信号的，只是整体被压低了**；
- 旁证：DIVE 的 **103 个正常合约**上，① 模型的合约级误报率只有 **0.65%**（@0.5 为 2.6%）——极度保守，正是上面这个成因的直接后果。

### 4.2 先排除"是不是我的处理管线坏了"

已逐条排查，都不是：

| 疑点 | 检查结果 |
| --- | --- |
| 图表加载对不对 | 7 棵树 + 12 棵变体树全部通过 `verify_channels="all"`（含逐通道 sha256 校验） |
| 标签有没有对错 | 修掉 `stem_key_of` 的 bug 后 **890/890** 全部匹配；逐类 support 与抽样清单一致 |
| 是不是模型输出死掉了 | 不是：概率有梯度（p50 0.097、p99 0.50、max 0.86），按类别看 AP 有高有低 |
| 是不是事件本身太难 | 不是：同一批图，`time_manipulation` / `uncheck` 两类**明显高于随机**（见 §4.3） |
| 是不是我改了阈值 | 没有：阈值逐字取自源语料 `thresholds.json`，**DIVE 上从未重搜** |

⇒ **是真实的跨数据集泛化现象**，不是管线故障。

### 4.3 🔴 最重要的发现：把 mAP 减去"随机基线"之后

**关键前提**：一个**完全随机**的排序器，它的期望 AP **恰好等于该类的正样本率**。
所以 DIVE 上"mAP = 0.40"听起来还行，其实**大部分是白送的**——因为 DIVE 的正样本率本来就高。

### 表 3b：逐类 AP 与随机基线的对照（阈值无关）

**① 模型 → DIVE**

| 类 | DIVE 正样本率（=随机 AP） | 逐类 AP | **AP − 随机** | 判定 |
| --- | --- | --- | --- | --- |
| access_control | 0.758 | 0.748 | **−0.010** | ❌ 低于随机 |
| arithmetic | 0.421 | 0.475 | +0.054 | ✅ 有信息 |
| dos | 0.152 | 0.174 | +0.023 | ≈ 与随机不可分 |
| front_running | 0.034 | 0.048 | +0.015 | ≈ |
| reentrancy | 0.525 | 0.570 | +0.045 | ≈ |
| time_manipulation | 0.260 | 0.373 | **+0.114** | ✅ 有信息 |
| uncheck | 0.274 | 0.396 | **+0.122** | ✅ 有信息 |
| **mAP** | **0.346** | **0.398** | **+0.052** | |

**对照：同一模型在 ① 自己的测试集上**（同一脚本 `--selfcheck` 复算，与 `evaluate.py` 既有产物**逐位相同**）

| | mAP | 随机基线 | **AP − 随机** |
| --- | --- | --- | --- |
| ① 同分布内测 | **0.7582** | 0.0652 | **+0.6930** |
| ① 模型 → DIVE | 0.3979 | 0.3462 | **+0.0516** |

**通俗说**：

- 在自己家里，模型比瞎猜强 **0.693**；
- 换到 DIVE，只比瞎猜强 **0.052** —— **只保住了 7.4%**。

**这一条推翻了"排序能力迁移住了、只是阈值失配"的直觉判断。**
只看 mAP 的绝对值（0.40 vs 0.30）会以为"跨数据集排序还行"；
**只有减去随机基线才看得出排序本身也大幅退化**。
如果论文只报 F1，会以为问题全在阈值；只报 mAP，会以为问题不大。**两者都报、并且减去基线，才看得见真相。**

### 4.4 消融结论能不能外推到 DIVE？——不能

看表 2 的 ΔDIVE 两列：

- **① 模型的 21 个臂全部挤在 ±0.013 内**（正典 std 就是 0.0167）⇒ **什么都分不出来**；
- **② 模型**有 `layers3` +0.139、`cb_rev` +0.067、`dfg_dep` +0.052 三个 `★`，
  但它们的 std 分别高达 ±0.116 / ±0.162 / ±0.040 —— 且 ② 在 DIVE 上的绝对水平只有 0.06，
  **这些"提升"是在一个近乎失效的工作点上测的，不具备可用性含义**。

⇒ **结论：本实验的消融排序不能外推到 DIVE。** 这不是方法的失败，而是**先验错配把整个工作点压垮了**，
导致所有臂都落进"几乎全判负"的退化区，彼此无从区分。

### 4.5 正常合约上的假阳性（大纲 5.5.1(3)）

| 模型 | 合约级误报率 @val_thr | @0.5 |
| --- | --- | --- |
| ① 模型 | **0.65%** | 2.59% |
| ② 模型 | 9.39% | 10.36% |

**通俗说**：① 模型在 DIVE 的正常合约上几乎不误报（很保守，代价是漏报极多）；
② 模型更"敢报"（因为它的训练语料正样本率本来就高），误报率约 9%。

---

## 5. 归因分层（大纲 5.5.1(5)：**不得单一归因于模型**）

| 层 | 本实验的证据 | 方向 |
| --- | --- | --- |
| **类别先验** | 逐类正样本率漂移 **25–43 倍**（表 4）；模型概率中位数 0.097 vs 真值 0.346 | ⬇⬇ **主因** |
| **标注口径** | DIVE 是**另一套标注来源**（`contract_labels.json`）；多标签占 **68.5%**，① 池内多标签近乎 0 | ⬇⬇ 语义可能不同 |
| **排序质量** | AP − 随机：+0.694 → +0.052（§4.3） | ⬇⬇ 真实退化 |
| **数据规模/结构** | DIVE 890 图介于 ① 453 与 ② 1774 之间；图规模 288 节点 vs ① 149 / ② 318 | → 中性 |
| **预处理/工具链** | DIVE 900 源 → 891 AST → **890 图**（过滤 1.1%）；`filter_report.txt` 按数据集分别计数 | → 影响很小 |
| **solc 版本分布** | 未做定量对照（**已知开口**） | ? |

**写法建议**：结论段必须写成"**先验错配 + 标注口径差异 + 真实泛化不足三者叠加**"，
并明确指出"其中有多少来自标注口径无法从本实验分离"。

---

## 6. 局限与披露（写论文时必须带上）

1. **DIVE 只抽样 900（实测 890）**，非全量 21696。协议是 `seed=0` 的**一次确定事件**，
   逐类 support 见 §7；`front_running` 仅 30 个正样本。
2. **每类 support 不均**：`dos` 135、`front_running` 30，逐类读数必须与 support 同看。
3. **①② 的 Δ 是 n=3 描述性**：判"干预有效/无效"须另做同配对 ≥9 点。
4. **主实验非逐位可复现**（CUDA 归约顺序），重跑抖动 ≈0.012，约为种子间 std 的 40%。
5. **`cb_unlimited` 无功效**（§3.3），`cb_rev`/`cb_unlimited` 还叠加"稀释"缺陷。
6. **DIVE 上的 `@val_thr` 一列几乎不可用**：阈值来自一个正样本率 ~3% 的验证集，
   应用到正样本率 35% 的测试集上。**这不是 bug，是先验错配的必然结果**，
   且按纪律**不允许**在 DIVE 上重搜阈值。故跨数据集的证据以**表 3b（阈值无关）**为准。
7. **编码器在训练标签上监督微调**，是任务特定表征，不是无监督改进。
8. **①②与 DIVE 三列禁止跨列比较绝对值**。
9. **未做**：DIVE 的 20–30 例 FN/FP 人工检查（大纲 5.5.1(4)）、solc 版本分布对照、
   SolidiFI 层次二。

---

## 7. DIVE 抽样集实测构成

| 项 | 值 |
| --- | --- |
| 抽样协议 | `seed=0`、均匀无放回、`n=900`（`decisions.md` §13 冻结，**不重抽**） |
| 解析结果 | 900 源 → 891 AST → **890 图**（过滤 1.1%） |
| 逐类正样本 | access_control **675**、arithmetic **375**、dos **135**、front_running **30**、reentrancy **467**、time_manipulation **231**、uncheck **244** |
| 多标签 | **610（68.5%）** |
| 正常合约（七类全 0） | **103** |
| 每类 ≥20 门槛 | ✅ 全部达标 |

> ⚠ 差额来自解析过滤（`assembly_gt50_filtered=4`、`slither_error_contracts=1`、`cfg_failed=1`…），
> 逐项见 `products/dive/raw/filter_report.txt`；按大纲 5.1 第一条须**按数据集分别**进透明性声明。

---

## 8. 复现命令

```bash
# 1) 构建 DIVE 图与特征（五步可单跑、可重入；约 5 h）
python scripts/build_dive_external_set.py --steps stage,raw,graphs,features,variants

# 2) 外部测试：21 臂 × 3 种子，两组语料
python scripts/evaluate_external.py --matrix main --out eval_results/dive/matrix_main.json
python scripts/evaluate_external.py --matrix aug  --out eval_results/dive/matrix_aug.json

# 3) 三方并列表（本文件的表 1–4）
python scripts/collect_dive_comparison.py

# 4) 内测消融表（含逐类 F1/P/R）
python scripts/collect_ablation_results.py --group main
python scripts/collect_ablation_results.py --group aug
```

**对拍验收**：`evaluate_external.py --selfcheck` 在源语料自己的内部测试划分上复算，
`micro@0.5 / @val_thr / macro@0.5 / mAP / 阈值 / 逐类 F1（7 类）` 与 `evaluate.py` 的既有产物
**全部 Δ = 0.00e+00**（`decisions.md` §38.3）。
