# 论文开发手册：M1、M3、M4、M5 实施指南

本文档面向新手，目标是把细化大纲中的 M1、M3、M4、M5 落成可执行工程。

说明：
- 你当前已经基本完成 M2 的异构图搭建，但本手册仍把 M2 单独写出来，因为 M1、M3、M4、M5 都依赖它的精确输出格式。
- 本手册只讲“按什么顺序做、输入是什么、输出是什么、格式是什么、每一步如何验收”。
- 如果你不懂深度学习，也可以照着一步一步执行；遇到实现细节，可以先保证数据格式正确，再继续往下做。
### 1.1 统一输入文件

你最终会遇到三类输入：

1. Solidity 源码文件 `.sol`
2. M2 生成的异构图 JSON 文件 `_hetero.json`
3. 标签文件 `contract_labels.json` / `solidifi_labels.json` / `buggy_contract_labels.json`

### 1.2 异构图 JSON 的标准格式

M2 的主输出是 `_hetero.json`，当前手册以它作为各模块之间的标准交换格式：

```json
{
  "meta": {...},
  "nodes": [...],
  "edges": {
    "CFG_FLOW": [...],
    "AST_PARENT": [...],
    "AST_PARENT_SAME": [...],
    "DFG_DEP": [...]
  }
}
```

每个节点至少包含：

```json
{
  "id": 1,
  "cfg_node_type": "EXPRESSION",
  "contract": "LogFile",
  "function": "AddMessage",
  "expression": "LastMsg.Sender = _adr",
  "span": [1553, 1574],
  "line_start": 90,
  "line_end": 90
}
```

每条边至少包含：

```json
{
  "source": 1,
  "target": 2
}
```

如果你要直接喂给当前仓库里的 PyG 代码，还需要再经过一次转换，生成 `_pyg.pt`，其中会保留 `cfg_node.x`、`node_id` 和四类关系的 `edge_index`。

### 1.7 M2 开发手册：CFG 中心异构图构建

M2 的目标不是训练模型，而是把编译产物整理成后续模块能稳定消费的图数据。当前仓库里这一步对应两个脚本：

1. `build_cfg_centered_hetero_graph.py`：从 AST、CFG、DFG 构建 `_hetero.json`
2. `convert_hetero_json_to_pyg.py`：把 `_hetero.json` 批量转成 `_pyg.pt`

### 1.7.1 M2 的输入

输入最少应包含：

1. AST JSON，来自编译或解析阶段
2. CFG dot 或等价 CFG 中间文件
3. DFG 文本输出
4. Solidity 源码根目录，用于回填表达式 span 和行号

### 1.7.2 M2 的输出

M2 建议输出两份结果：

1. 结构化图文件 `_hetero.json`
2. PyG 文件 `_pyg.pt`，供后续模型直接读取

`_hetero.json` 的顶层结构应保持固定：

```json
{
  "meta": {
    "source": "...",
    "source_path": "...",
    "cfg_node_count": 0,
    "cfg_edge_count": 0,
    "ast_edge_count": 0,
    "ast_same_cfg_edge_count": 0,
    "ast_unmapped_edge_count": 0,
    "dfg_edge_count": 0,
    "dfg_parse_errors": 0,
    "expr_span_total": 0,
    "expr_span_missing": 0
  },
  "nodes": [...],
  "edges": {
    "CFG_FLOW": [...],
    "AST_PARENT": [...],
    "AST_PARENT_SAME": [...],
    "DFG_DEP": [...]
  }
}
```

### 1.7.3 M2 的详细步骤

#### 步骤 1：读取 AST / CFG / DFG / 源码

输入：

- AST JSON
- CFG dot 文件
- DFG 文本文件
- 合约源码

处理：

- 读取 AST 根节点和合约信息
- 解析 CFG 节点类型和表达式
- 解析 DFG 中的变量依赖文本
- 回填源码路径和源文件内容

输出：

- 一组待映射的原始编译产物

验收：

- 同一个样本能同时找到 AST、CFG、DFG 三类输入
- 源码路径存在且能读到文本

#### 步骤 2：构建 CFG 中心节点表

输入：

- CFG 节点及边

处理：

- 为每个 CFG 节点分配全局 `id`
- 记录 `cfg_node_type`
- 记录 `contract`、`function`、`expression`
- 尽量回填 `span`、`line_start`、`line_end`

输出：

- `nodes` 列表

验收：

- 每个节点都有稳定的整数 `id`
- 节点缺少 `expression` 时也能继续生成图

#### 步骤 3：生成 CFG_FLOW 边

输入：

- CFG 节点局部边

处理：

- 把局部节点编号映射到全局 `id`
- 仅保留有效的 CFG 后继关系

输出：

- `CFG_FLOW` 边集合

验收：

- `meta.cfg_edge_count` 与 `CFG_FLOW` 边数一致

#### 步骤 4：映射 AST 边

输入：

- AST 节点和 AST 父子边

处理：

- 把 AST 节点尽量映射到对应的 CFG 节点
- 若两个 AST 节点映射到同一个 CFG 节点，记为 `AST_PARENT_SAME`
- 无法映射的 AST 边单独计数到 `ast_unmapped_edge_count`

输出：

- `AST_PARENT` 和 `AST_PARENT_SAME` 边集合

验收：

- AST 映射失败的数量是可见的，而不是静默丢失

#### 步骤 5：生成 DFG_DEP 边

输入：

- DFG 文本解析结果

处理：

- 解析变量定义和使用关系
- 将依赖关系映射到 CFG 节点对

输出：

- `DFG_DEP` 边集合

验收：

- `meta.dfg_edge_count` 与 `DFG_DEP` 边数一致

#### 步骤 6：序列化并转换为 PyG

输入：

- `nodes`
- `edges`
- `meta`

处理：

- 写出 `_hetero.json`
- 调用 `convert_hetero_json_to_pyg.py` 生成 `_pyg.pt`

输出：

- 两份可复用的图产物

验收：

- `convert_hetero_json_to_pyg.py` 能正确读出 `cfg_node.x`
- `node_id`、`contract`、`function`、`expression` 被保留用于追踪

### 1.7.4 M2 的新手检查清单

1. `_hetero.json` 的 `nodes` 数量是否和 `meta.cfg_node_count` 一致。
2. 四类边是否都存在，且边数不是全 0。
3. `span` 和 `line_start/line_end` 是否能在样例图上回填出来。
4. `_pyg.pt` 是否能被后续模型直接加载。
  "contract": "LogFile",
  "function": "AddMessage",
  "expression": "LastMsg.Sender = _adr",
  "span": [1553, 1574],
  "line_start": 90,
  "line_end": 90
}
```

每条边至少包含：

```json
{
  "source": 1,
  "target": 2
}
```

### 1.3 M1 需要的输入格式

M1 最理想的输入不是整份原始 JSON，而是一个“可查询上下文”：

```python
{
  "nodes": {
    node_id: node_dict,
    ...
  },
  "adj_cfg": {
    node_id: [successor_id1, successor_id2, ...],
    ...
  },
  "state_vars": set([...]),
  "bool_consumed_nodes": set([...])
}
```

你可以把它理解成：

- `nodes`：每个节点的详细信息
- `adj_cfg`：只保留 CFG_FLOW 的后继关系
- `state_vars`：状态变量白名单
- `bool_consumed_nodes`：返回值是否被消费的预计算集合

### 1.4 M3 需要的输入格式

M3 的输入是：

```text
G_hetero + 节点源码片段 + M1 先验分数 s_v
```

更具体地说，M3 每个节点需要这些数据：

```json
{
  "node_id": 9,
  "cfg_node_type": "IF",
  "expression": "msg.sender.call.value(_am)()",
  "span": [1006, 1034],
  "line_start": 54,
  "line_end": 54,
  "s_v": 0.43
}
```

### 1.5 M4 需要的输入格式

M4 需要：

- M3 输出的 `h_v^(0)` 或 `h_v^(L)`
- 异构图的边索引
- 边类型信息

### 1.6 M5 需要的输入格式

M5 训练时至少需要：

- 图级输入 `h_G`
- 节点级掩码 `m_v`
- 图级标签 `Y_G`
- M1 的先验分数 `s_v`

---

## 2. M1 开发手册：语义锚点检测

### 2.1 M1 的目标

M1 的目标不是“判断是否一定有漏洞”，而是“找出值得重点关注的节点”。

换句话说：

- 它输出的是先验提示，不是最终判决。
- 它会给每个节点打七类布尔标志。
- 它还会把这些布尔标志压成一个 0 到 1 之间的分数 `s_v`。

### 2.2 M1 的输入

输入最少应包含：

1. 节点表 `nodes`
2. CFG 邻接 `adj_cfg`
3. 状态变量白名单 `state_vars`
4. 节点是否在 `unchecked{}` 中的标记

### 2.3 M1 的输出

M1 建议输出两份结果：

1. 节点级布尔标志表

```json
{
  "9": {
    "reentrancy": true,
    "arithmetic": false,
    "access_control": true,
    "uncheck_return": true,
    "dos": false,
    "front_running": false,
    "time_manipulation": false
  }
}
```

2. 节点级先验分数表

```json
{
  "9": 0.4285714286
}
```

### 2.4 M1 的详细步骤

#### 步骤 1：把异构图 JSON 读进来

输入：

- 一个 `_hetero.json` 文件

处理：

- 读取 `meta`
- 读取 `nodes`
- 读取 `edges`

输出：

- 原始 JSON 对象

验收：

- 你能看到节点数、边数、文件名这些基本信息。

#### 步骤 2：把 JSON 转成 GraphContext

输入：

- 原始 JSON

处理：

- 把 `nodes` 转成 `node_id -> node_dict`
- 只保留 `CFG_FLOW` 建邻接表
- 读入状态变量白名单
- 预计算哪些节点的返回值已经被消费

输出：

- 一个 GraphContext 对象

验收：

- `ctx.nodes` 里能查到节点 9 的表达式 `msg.sender.call.value(_am)()`
- `ctx.adj_cfg` 里能查到 CFG 后继

#### 步骤 3：给每个节点准备表达式和类型

输入：

- `ctx.nodes[node_id]`

处理：

- 取 `expression`
- 取 `cfg_node_type`
- 取 `is_unchecked` 或同类标志

输出：

- 供规则判断使用的节点属性

验收：

- 空表达式节点不会报错

#### 步骤 4：逐类写锚点规则

这一步是 M1 的核心。

七类规则与大纲对应关系如下：

1. reentrancy：低级 call / send / transfer
2. arithmetic：unchecked 块中的算术运算
3. access_control：危险操作和权限变量共现
4. uncheck_return：send/call 返回值没有被消费
5. dos：循环体内外部调用，BFS 深度不超过 2
6. front_running：竞态状态变量的写入节点
7. time_manipulation：block.timestamp / now 读取

输出：

- 每类一个布尔函数

验收：

- 节点 9 应命中 reentrancy
- 节点 3 应命中 time_manipulation
- 节点 18 `intitalized = true` 不应被误判为前跑漏洞

#### 步骤 5：计算七类布尔标志

输入：

- `node_id`
- `ctx`

处理：

- 对该节点依次调用七类规则

输出：

- 七个布尔值组成的字典

验收：

- 字典一定有七个键

#### 步骤 6：把布尔标志压成先验分数

输入：

- 七类布尔标志

处理：

- 命中几个类，就除以 7

输出：

- `s_v ∈ [0, 1]`

验收：

- 全不命中时 `s_v = 0`
- 全命中时 `s_v = 1`

#### 步骤 7：批量导出

输入：

- 一个 GraphContext

处理：

- 遍历所有节点
- 计算 flags 和 score

输出：

- 节点级先验文件，建议保存为 `.json`

建议格式：

```json
{
  "node_flags": {...},
  "node_scores": {...}
}
```

### 2.5 M1 的新手检查清单

你可以先拿样例图做这个检查：

1. `LastMsg.Time = now` 是否命中 time_manipulation。
2. `msg.sender.call.value(_am)()` 是否命中 reentrancy。
3. `MinSum = _val` 是否被误判为前跑漏洞。
4. `revert()()` 这种节点是否被错误当成 return 检查。

如果这 4 个检查不稳定，先不要做后面的 M3-M5。

---

## 3. M3 开发手册：节点特征初始化

### 3.1 M3 的目标

M3 的目标是把每个节点变成一个神经网络可以处理的向量。
- 规则系统 M1 负责“指出哪里可疑”
- M3 负责“把可疑节点变成数字表示”

### 3.2 M3 的输入

输入是：

1. 异构图 `G_hetero`
2. 节点源码文本 `expression`
3. 节点 span 或 line 信息
4. M1 的先验分数 `s_v`

### 3.3 M3 的输出

输出是每个节点的初始特征向量：

```text
h_v^(0) ∈ R^256
```

也就是每个节点对应一个 256 维向量。

### 3.4 M3 的详细步骤

#### 步骤 1：准备节点文本

输入：

- 节点的 `expression`
- 节点 `span`
- 节点在 CFG/DFG 中的上下文

处理：

- 取目标节点文本
- 取 DFG 前驱文本
- 取 CFG 后继文本

输出：

- 一段拼好的文本上下文

建议格式：

```text
[TGT] 当前节点表达式 [/TGT]
[DFG_PRE] 前驱表达式1 ... [/DFG_PRE]
[CFG_POST] 后继表达式1 ... [/CFG_POST]
```

验收：

输入：

- 拼好的文本

处理：

- 目标节点占 60%
输出：

- 限定长度的文本序列

验收：
#### 步骤 3：做节点类型嵌入

输入：

- 节点类型

处理：

- 把 7 类节点类型映射成可学习向量

输出：

- `TypeEmbed`，维度 64

验收：

- 同一类型的节点初始向量在该通道上应一致

#### 步骤 4：做 CodeBERT 语义嵌入

输入：

- 第 1 步准备好的文本

处理：

- 送入 CodeBERT 编码
- 取池化后的 256 维表示

输出：

- `h_CB ∈ R^256`

验收：

- 文本不同，向量应该明显不同

#### 步骤 5：做轻量类型属性嵌入

输入：

- 变量类型
- 是否状态变量
- 是否低级调用
- 是否在 unchecked 块
- 函数可见性

处理：

- 把这些属性变成一个 32 维向量

输出：

- `type_attr ∈ R^32`

验收：

- 缺失属性时必须有默认值，不许报错

- `type_attr`

处理：
输出：

- `type_attr_dropped`
验收：

- 训练时输出应有随机变化
#### 步骤 7：做 FiLM 融合

输入：

- `TypeEmbed`
- `type_attr_dropped`

处理：

- `h_fused ∈ R^256`


- 语义通道和类型通道都在结果里有贡献

输入：

- `TypeEmbed` 64 维
- `h_fused` 256 维
- `edge_stat` 8 维
处理：

- 拼接后通过线性层压到 256 维

- `h_v^(0) ∈ R^256`


- 每个节点都输出固定维度向量


1. 随便取一个节点，看它的输入文本是不是和源码一致。
2. 看 `h_v^(0)` 维度是不是恒定 256。
3. 看同类型节点是不是会因为 dropout 在训练中变动。
4. 看缺 span 的节点是不是也能正常跑完。
---

## 4. M4 开发手册：HGT 消息传递 + 子图选择
M4 的目标是：

1. 让节点之间的信息沿异构边传播

### 4.2 M4 的输入

2. `h_v^(0)`
3. 边类型信息

### 4.3 M4 的输出

2. 节点掩码 `m_v`
3. 图级表示 `h_G`

#### 步骤 1：确定异构关系类型

输入：
- `CFG_FLOW`
- `AST_PARENT`
- `DFG_DEP`
处理：

- 每种边类型单独建消息通道

输出：
验收：

- 三类边都能被识别

建议固定为：

- 隐藏维度 `d = 256`
- 注意力头数 `4`
- DropEdge `0.15`
输出：

- 一套固定的训练配置

验收：

- 训练日志里能看到这些参数

#### 步骤 3：做关系门控

输入：

- 每种边类型的消息

处理：

- 给每种边一个可学习门控值 `gate_r`

输出：

- 门控后的消息权重

验收：

- 门控值不是常数，训练后会变化

#### 步骤 4：跑两层消息传递

输入：

- `h_v^(0)`
- 边索引
- 门控值

处理：

- 第 1 层传播
- 第 2 层传播

输出：

- `h_v^(2)`

验收：

- 所有节点向量仍保持 256 维

#### 步骤 5：计算节点掩码分数

输入：

- `h_v^(2)`

处理：

- 用一个小 MLP 输出每个节点的 `logit_v`

输出：

- 节点掩码分数

验收：

- 每个节点有一个标量分数

#### 步骤 6：训练时使用 Gumbel-Softmax

输入：

- `logit_v`
- 温度 `τ_mask`

处理：

- 加 Gumbel 噪声
- 生成软掩码 `m_v`

输出：

- 训练态软掩码

验收：

- 掩码值一般在 0 到 1 之间

#### 步骤 7：计算稳定性损失

输入：

- 同一图两次不同噪声下的 mask

处理：

- 计算两次 mask 的均方误差

输出：

- `L_stab`

验收：

- 前 10 个 epoch 可以不启用，之后启用

#### 步骤 8：推理时硬二值化

输入：

- `logit_v`

处理：

- `logit_v > 0` 记为 1，否则记为 0

输出：

- 硬掩码 `m_v`

验收：

- 推理结果稳定、可复现

#### 步骤 9：图级 readout

输入：

- 掩码后的节点表示

处理：

- 先按节点类别池化
- 再拼接成图向量

输出：

- `h_G ∈ R^256`

验收：

- 每个合约得到一个图向量

### 4.5 M4 的新手检查清单

1. 看门控值是不是会变化。
2. 看掩码是不是能把明显无关节点压低。
3. 看训练前 10 轮掩码是不是比较发散，后面逐渐稳定。
4. 看图向量维度是不是固定 256。

---

## 5. M5 开发手册：两阶段训练

### 5.1 M5 的目标

M5 的目标是把前面的模块真正训练起来，并且证明它不是“只会背训练集”。

### 5.2 M5 的输入

1. `h_G`
2. `m_v`
3. 图级标签 `Y_G`
4. 先验分数 `s_v`

### 5.3 M5 的输出

1. 分类预测结果
2. 最终子图 `G*`
3. 训练日志和评估指标

### 5.4 M5 的详细步骤

#### 步骤 1：准备标签文件

输入：

- `contract_labels.json` 或 `solidifi_labels.json`

处理：

- 读取每个合约的漏洞标签

输出：

- 图级标签 `Y_G`

验收：

- 每个图必须能对应到标签

#### 步骤 2：定义损失函数

输入：

- 图级预测
- 掩码
- 先验分数
- 两次 mask 的稳定性结果
- 对比学习样本对

处理：

- 组合成总损失

输出：

- `L_total`

建议形式：

```text
L_total = L_graph + λ_s·L_sparse + λ_p·L_prior + λ_stab·L_stab + λ_c·L_contrastive
```

#### 步骤 3：设置优化器和学习率

建议配置：

- 优化器：AdamW
- 学习率：3e-4
- 权重衰减：1e-4
- 梯度裁剪：1.0
- batch size：32
- 最大 epoch：80

验收：

- 训练过程不会频繁爆炸或 NaN

#### 步骤 4：Phase 1 联合训练

输入：

- 全部训练图

处理：

- 一边训练分类头，一边优化先验、稀疏、稳定性、对比损失

输出：

- 初步收敛的模型参数

验收：

- 验证集 F1 应该随 epoch 上升并逐渐稳定

#### 步骤 5：温度退火

输入：

- 当前 epoch

处理：

- 每 10 个 epoch 让 `τ_mask` 下降一次

输出：

- 逐渐更尖锐的 mask

验收：

- 稀疏度从接近随机逐渐变得更稳定

#### 步骤 6：Phase 2 可选微调

输入：

- Phase 1 训练好的模型

处理：

- 冻结主体
- 用软伪标签微调节点分类头

输出：

- 可选增强模型

验收：

- 不能明显破坏图级性能

#### 步骤 7：记录训练稳定性

输入：

- 每个 epoch 的 loss、mask、F1

处理：

- 输出训练日志

输出：

- 训练曲线

验收：

- 至少能画出 loss 曲线、mask 稀疏度曲线、验证集 F1 曲线

### 5.5 M5 的新手检查清单

1. 训练时有没有 NaN。
2. 验证集 F1 有没有提升。
3. 掩码是不是越来越稳定。
4. 训练集和验证集差距是不是过大。

---

## 6. 建议你按这个顺序实际开发

如果你从零开始，建议按下面顺序做，不要跳步：

1. 先把异构图 JSON 格式固定住。
2. 再修 M1 的输入适配器，让它能读当前 hetero.json。
3. 用样例图把 7 类规则全部跑通。
4. 导出 `node_flags` 和 `node_scores`。
5. 再做 M3 的节点文本拼接和特征向量化。
6. 然后接 M4 的 HGT 和 mask。
7. 最后做 M5 的训练和评估。

这条顺序的原因很简单：

- 数据格式错了，后面所有模块都会错。
- M1 不稳定，M5 的先验损失就没有意义。
- M3 不稳定，M4 学不到东西。
- M4 不稳定，M5 的掩码和子图解释就不可信。

---

## 7. 你最容易犯的错误

1. 把“原始 Slither 节点类型”当成“论文里的 7 类节点类型”。这两个不是一回事。
2. 以为只要有 JSON 就能直接喂给 M1。实际上 M1 需要可查询上下文，不是原始文件本身。
3. 只做模型，不做输入校验。新手最常见的问题就是训练到一半才发现字段缺失。
4. 不区分训练态和推理态。尤其是 dropout 和 Gumbel-Softmax，训练和推理的行为不一样。
5. 把 Synthetic 结果当成真实世界泛化能力。文档已经要求这两类评估分开，你必须照做。

---

## 8. 最低可用交付标准

如果你想先交一个“能跑”的版本，最少要满足这些条件：

1. M1 能对样例图输出 7 类 flags 和 score。
2. M3 能把每个节点变成 256 维向量。
3. M4 能输出图向量和节点 mask。
4. M5 能完成一次完整训练并产生日志。
5. 所有步骤都能读写明确的 JSON / tensor 格式，不靠手工改文件。

---

## 9. 你下一步最该做什么

建议你下一步先做两件事：

1. 先修 M1 的输入适配，让它能直接读取当前 `_hetero.json`。
2. 再为样例图写一个最小验证脚本，逐个检查 `LastMsg.Time = now`、`msg.sender.call.value(_am)()`、`MinSum = _val` 这些节点的规则命中情况。

只要这一步稳定，后面的 M3-M5 才有可靠基础。