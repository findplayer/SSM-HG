SSM-HG/
├── 论文开发手册.md / Todo_List.md / 研究点一细化大纲改I.docx
│
├── scripts/                      # 会跑很多遍的脚本（已收拢到这，统一从仓库根运行）
│   ├── build_cfg_centered_hetero_graph.py   # M2：构建 _hetero.json（已完成）
│   ├── dump_cfg.py                          # CFG 三子类导出（已完成）
│   ├── convert_hetero_json_to_pyg.py        # M2 → _pyg.pt（已完成）
│   ├── priori_scoring.py                    # M1 规则（已完成；dos 锚点②含 push 与 array.length 自增两类无界增长模式）
│   ├── m1_runner.py                         # M1 批量打分 → _m1.json（已完成）
│   ├── m3_build_features.py                 # 新建（M3）→ _cb.pt + {safe}_feat.pt
│   │                                        #   _feat.pt = MLP 后的 128 维 h_v(0)（唯一模型输入特征，语义锁死）
│   │                                        #   不写回 _pyg.pt；--only <图前缀> 单样本验证；--variant 消融变体
│   ├── model.py                             # 新建（M4）SSMHG 骨架（手册 9.5）+ 随机数据 smoke test
│   ├── dataset.py                           # 新建（M5）数据层：_pyg.pt(结构) + _feat.pt(x) + _m1(s_v)
│   │                                        #   + 标签对齐(加载断言) + 边级消融开关（不再过 MLP）
│   ├── metrics.py                           # 新建（M5）指标层：宏/微 F1、每类 P/R/F1、mAP
│   ├── make_splits.py                       # 新建（M5）→ splits/split_seed{seed}.json
│   │                                        #   + split_report.json + unmatched_contracts.txt
│   ├── train.py                             # 新建（M5）→ runs/seedN/{log.txt, best.pt, config.json}
│   ├── evaluate.py                          # 新建（M5）→ runs/seedN/results.json
│   │                                        #   --task ablation/baseline → eval_results/
│   └── generate_all_ast_cfg_dfg.sh          # 批量生成 raw（已改好）
│   # 数据流原则：_pyg.pt 只读（纯结构）；_feat.pt 是 M3 唯一输出特征；dataset.py 只组合不再算
│
├── raw/                          # 全量解析原始产物（已生成 581 个合约）
│   ├── AST-raw/  {safe}.json
│   ├── CFG-raw/  {safe}__*.dot , {safe}__cfgdetail.json
│   ├── DFG-raw/  {safe}_dfg.txt
│   ├── logs/      ast_error.log / cfg_error.log / dfg_error.log（日志与产物分离）
│   └── filter_report.txt                     # 过滤统计（论文透明性声明用）
│
├── Heterogeneous graphs/         # 全部图产物，同名前缀对齐（hetero/m1/pyg 各 581 已生成）
│   ├── {safe}_hetero.json        # M2 修订版
│   ├── {safe}_m1.json            # M1
│   ├── batch_summary.json        # M1 批量统计（含 total_class_hits / class_hit_graphs 七类聚合）
│   ├── {safe}_pyg.pt             # 只读结构：edge_index/edge_type/node_id/meta/label（x 为 N×1 占位，永不改写）
│   ├── {safe}_cb.pt              # M3 CodeBERT 缓存（手册 8.4.3）
│   ├── {safe}_feat.pt            # M3 唯一输出：MLP 后的 128 维 h_v(0)（语义锁死；dataset 只组合不重算，可覆盖重跑）
│   └── {safe}_feat_{variant}.pt  # M3 消融变体输出（--variant no-prior/no-codebert，复用 _cb.pt 缓存）
│
├── splits/                       # M5（make_splits.py 生成）
│   ├── split_seed{0,1,2}.json    #   train/val/test 合约名列表
│   ├── split_report.json         #   每类正负样本数/正负比/唯一合约/多标签/全 0 统计（手册 10.2.4）
│   └── unmatched_contracts.txt   #   标签匹配不上的合约（记录并报告，不静默丢弃，手册 10.2.2）
├── runs/                         # M5 主实验（3 种子）
│   ├── seed{0,1,2}/              #   log.txt / best.pt / results.json（固定 0.5 + 搜索阈值双报告）
│   │                             #   config.json（本次运行参数快照，实验可追踪）
│   └── summary.json              #   ≥3 种子均值 ± 标准差汇总
├── eval_results/                 # 仅阶段 5：消融 + 基线 + SmartBugs/SolidiFI 结果
│
├── alldata(readonly)/            # 只读：alldata_sol_source/ + contract_labels.json（已有）
└── MVD-HG-dataset/               # 只读：单类标签核对用（已有）

# 状态（2026-09-05）：M1/M2 审计收尾关闭；FindThisHash（constant 猜谜竞态）front_running 无形态锚点，记为已知局限不扩规则；下一步进入 M3（m3_build_features.py）。

