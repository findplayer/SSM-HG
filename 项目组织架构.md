SSM-HG/  —— 目录结构与状态（2026-09-12 重构后；目录变化只改本文件与手册 §3.2）
│   文档层级：研究点一细化大纲改II.docx（最高权威）→ 论文开发手册.md（实施细节）→ Todo_List.md（任务状态）→ 本文件（目录与状态记录）
│
├── 研究点一细化大纲改II.docx      # 论文大纲；口径冲突一律以“改II”为准
├── 论文开发手册.md                # §3.2 路径速查 / §12 常见错误清单
├── Todo_List.md                   # 分阶段任务与验收状态
├── 项目组织架构.md                # 本文件
├── AGENTS.md                      # 全工具共享代理指令（跨工具开放标准；VS Code/Copilot 直接读取）
├── CLAUDE.md                      # Claude Code 入口（仅 @AGENTS.md 导入；内容单一维护在 AGENTS.md）
├── .gitignore                     # 忽略 __pycache__/、*.pyc、slither-env/、DIVE/Code-based.csv（147 MB，超单文件限制）、
│                                  # alldata_augmentation/（只读源，2026-09-14 起防「一键提交」误纳入）；
│                                  # products/**/raw/* 与 products/**/graphs/*（体积大）——**但白名单纳入**
│                                  #   `!raw/filter_report.txt`（过滤透明性声明）与 `!graphs/ir_cat.json`（冻结 IR 字典，语义锚点，几 KB）；
│                                  # runs/**/*.pt——**但白名单纳入** `best.pt`（evaluate 唯一权重输入）+
│                                  #   `val_best_probs.pt`/`test_probs.pt`（推理缓存，共 0.3 MB），
│                                  #   使指标可**离线重算、无需重训**（2026-09-16 起；全套 115 MB，单文件 ≤4.77 MB）；
│                                  #   `last.pt` 仍排除（仅断点续训用，与报告数字无关，入册会使体积翻倍）
│                                  # products/**/graphs_ft*/**、products/**/graph_variants/** 与 products/**/src_stage/*（**§37/§38 新增形态，2026-09-20 补规则**）：
│                                  #   写法必须 `dir/**` + `!dir/**/` + `!具体文件名`（`dir/*` 与 `dir/` 都**不能**配合 `!` 白名单）；
│                                  #   只纳入各变体的 `variant.json`（自述文件，**40 个 / 内容合计 31 KB**，2026-09-20 实测）
│                                  #   🔴 **2026-09-21 泛化为前缀通配 `graphs_ft*`**：任务2 新增 `graphs_ft_buggy/`，
│                                  #   字面量 `graphs_ft` **匹配不到它** ⇒ 又与下面 `codebert_ft` 同款的洞（同一天第三次）
│                                  # runs/codebert_ft*/**（**§37 正典的微调编码器**，2026-09-20 补规则；2026-09-21 泛化）：
│                                  #   实测 **2.9 GB**、单文件 `pytorch_model.bin` **498 MB**（超本仓「不提交 >100 MB」硬规则）；
│                                  #   只留 `corpus.json`/`config.json`/`special_tokens_map.json`/`tokenizer_config.json` 四类小边车；
│                                  #   🔴 2026-09-21 新增的 `runs/codebert_ft_buggy/`、`runs/codebert_ft_probe/` 同样不被字面量匹配，
│                                  #   实测 `git add -A --dry-run` 达 **42 文件 / 958.5 MB**（两份 475 MB 权重）⇒ 改前缀通配后 **34 文件 / 1.02 MB**
│                                  # 其余 JSON 实验记录（config/log/results/thresholds/summary/diagnosis）全部纳入
│                                  # runs/ablation_n9*/**/best.pt 与 runs/arch_n9*/**/best.pt（**2026-09-21 用户裁定，例外之例外**）：
│                                  #   n=9 复现研究区**只入概率缓存、不入权重**——全部指标可由 test_probs.pt 离线重算，
│                                  #   而全入会让入库 runs/ 由 452 MB 涨到约 2.0 GB。规则写在 `!runs/**/best.pt` **之后**才生效；
│                                  #   ⚠ 代价：无 best.pt 不能从零重跑 evaluate.py；可接受——每臂的对角 3 对仍在
│                                  #   `runs/ablation{,_aug}/` 且保留权重。三步自检实测：**1084 文件 / 7.8 MB / 最大 136 KB**
│
├── scripts/                       # 全部流水线脚本（统一从仓库根运行：python scripts/xxx.py）
│   ├── generate_all_ast_cfg_dfg.sh          # 批量生成 → products/alldata/raw/{AST-raw,CFG-raw,DFG-raw}
│   ├── build_cfg_centered_hetero_graph.py   # M2：_hetero.json 构图（已完成；默认 in/out = products/alldata/）
│   ├── dump_cfg.py                          # CFG 三子类导出（已完成）
│   ├── priori_scoring.py                    # M1 规则（已完成）
│   ├── m1_runner.py                         # M1 批量打分 → _m1.json（已完成）
│   ├── convert_hetero_json_to_pyg.py        # M2 → _pyg.pt（已完成）
│   ├── m3_build_features.py                 # M3：_cb.pt + _feat.pt 通道（已完成；纯通道构建，无 RNG/变体）
│   ├── model.py                             # M4：SSMHG（已完成；RGCN/GCN 两层、h2-based Readout）
│   ├── dataset.py                           # M5 数据层（已完成：通道字典+_cb.pt 对齐+标签+边级消融）
│   ├── make_splits.py                       # M5 划分（已完成）→ products/alldata/splits/
│   ├── audit_data_funnel.py                 # 口径溯源审计（2026-09-12）→ docs/data_funnel.md + splits/data_funnel.json
│   ├── audit_cb_func_gap.py                 # 函数级通道缺口审计（2026-09-12）→ splits/cb_func_gap{,_after}.json + docs/cb_func_gap{,_after}.md
│   ├── audit_graph_fingerprint.py           # 逐图指纹（2026-09-12 R5）→ 验证“只有 N 图被改，其余逐位不变”（只读）
│   ├── run_guard.py                         # **输出目录覆盖守卫**（2026-09-16）：train/make_splits/m3 共用，
│   │                                        #   防换数据集或做消融时无声销毁正典结果（decisions §25.1）
│   │                                        #   ⚠ 只比对**参数**：不改 CLI 参数的口径修复（§28）它会放行覆盖
│   ├── rerun_from_config.py                 # **配置驱动重放**（2026-09-17，decisions §28）：以归档 config.json::args
│   │                                        #   为唯一事实来源重建 train/evaluate 命令行；--evaluate-only / --summarize
│   ├── aggregate_results.py                 # 按臂聚合 runs/**/results.json → 可粘贴的 Markdown 表（2026-09-17）
│   ├── paired_study_analysis.py             # 研究臂**同配对**分析（n=9）：Δ + 配对 t（2026-09-17，decisions §26.7/§27.5）
│   ├── error_rates.py                       # **误报率/漏报率**三层口径（2026-09-17，decisions §30）：只读 test_probs.pt
│   │                                        #   → runs/error_rates.json；L3 合约级 ≡ 二分类视图（结论卷 §9）
│   ├── run_study.py                         # **n=9 同配对驱动**（2026-09-17，decisions §31）：唯一支持
│   │                                        #   (train_seed, split_seed) 独立配对的运行器（run_ablation 把
│   │                                        #   split_seed 绑死等于 seed）。import 复用其单变量断言，
│   │                                        #   另加「一对两臂语料四键逐字相同」断言 → runs/binary_arm/
│   ├── run_cbft_study.py                    # **微调 CodeBERT 的 n=9 同配对复核**（2026-09-19，decisions §36）：
│   │                                        #   ts∈{0,1,2}×ss∈{0,1,2}，两臂 = frozen / cbft（**变量即 graph_dir**）。
│   │                                        #   三条开跑前断言：单变量 + 同语料（**语料键不含 graph_dir**）+
│   │                                        #   产物层（590 图 `_feat` 三通道 sha256 相同、`_cb` 全不同、结构软链）。
│   │                                        #   复用 run_ablation 四函数 + run_study.argv_for_diagnose → runs/cbft_study/
│   ├── repair_augmentation_labels.py        # 增强集标签修正（2026-09-14，decisions §19.3.1）→ products/augmentation/{contract_labels_repaired.json,label_repair_report.json}（不写只读源）
│   ├── recompact_cb_cache.py                # `_cb.pt` 紧凑化（2026-09-18，decisions §33）：把"视图条目"重存为紧凑副本，
│   │                                        #   值逐位不变、体积 ×39；先写临时文件+重载逐位比对再 os.replace。
│   │                                        #   存量已跑：主库 15.586 GB → 0.403 GB（增强集本就紧凑，未动）
│   ├── build_graph_variant.py               # 造消融用的**图变体目录**（2026-09-18，decisions §34）：callback_rev /
│   │                                        #   callback_unlimited / cb_ft_ss{S}（⚠ 2026-09-19 后 cb_ft 已**改名为
│   │                                        #   graphs_ft/ss{S} 并升为正典**，见 §37；旧名留兼容软链）。真产物只算必要的那几件、其余**软链**
│   │                                        #   （产物层的"唯一变量"），并逐图断言 `_feat.pt` 该同的同、该异的异 →
│   │                                        #   products/<数据集>/graph_variants/<变体>/{变体产物, variant.json}
│   ├── build_ft_edge_variants.py            # 造「**边变体 × 微调编码器**」组合变体（2026-09-19，decisions §37）：
│   │                                        #   微调升为正典后，cb_rev/cb_unlimited 的旧变体带的是**冻结** _cb.pt
│   │                                        #   ⇒ 会变成两个变量。**逐图分流**：三通道未变者软链、被边改动者真跑 M3 →
│   │                                        #   products/<语料>/graphs_ft/graph_variants/{cb_rev,cb_unlimited}_ss{S}/
│   ├── finetune_codebert.py                 # 阶段 1 微调 CodeBERT（2026-09-18，decisions §34）。
│   │                                        #   **2026-09-19 起它是「正典」的一部分，不再是消融项**（decisions §37）：
│   │                                        #   函数源码文本（**复用 m3 的口径**）→ 合约内 mean-pool → 7 维头；
│   │                                        #   只用 train 划分（硬断言无泄漏），判据 val macro-F1 →
│   │                                        #   runs/codebert_ft/<语料>/ss{S}/encoder/（可直接喂 m3 --codebert；**按语料隔离**，见 decisions §35.2）
│   ├── collect_ablation_results.py          # 消融结果**程序复算**汇总（不手抄）→ eval_results/ablation/collected{,_aug}.{json,md}
│   │                                        #   两张表：绝对值 mean±std 与 Δ相对正典（★=超正典种子间 std）
│   ├── run_remaining_ablations.sh           # 剩余消融的**串行队列**（可重入：变体看 variant.json、微调看 encoder/、
│   │                                        #   训练看 best.pt）→ bash scripts/run_remaining_ablations.sh {main|aug}
│   ├── sample_dive_subset.py                # DIVE 外部测试子集抽样（2026-09-12；seed=0/n=900/均匀）→ products/dive/splits/
│   ├── build_dive_external_set.py           # DIVE 外部测试集的**图与特征**构建（2026-09-19；五步可单跑/可重入：
│   │                                        #   stage→raw(Slither)→M2/M1/PyG→M3→边变体）。编码器矩阵：图结构**一份**，
│   │                                        #   特征**三套**（冻结 / ① 微调 / ② 微调）→ products/dive/graphs{,_ft,_ft_aug}/
│   ├── evaluate_external.py                 # **外部测试**推理与报告（2026-09-19）：把训练好的模型原样搬到另一数据集。
│   │                                        #   🔴 阈值**只读**源语料 thresholds.json，绝不在目标集重搜（大纲 5.1）。
│   │                                        #   `--matrix {main,aug}` 跑完 21 臂×3 种子；`--selfcheck` 与 evaluate.py 对拍
│   ├── map_solidifi_injections.py           # SolidiFI 注入位置（`buggy_logs/*.csv` 的 loc）→ CFG 节点映射
│   │                                        #   （大纲 5.2 层次二；规则=line_start≤loc≤line_end，多命中取行区间最小者；
│   │                                        #   `bug type`→七类的映射表已用"反推合约标签 350/350 全对"证实）
│   ├── evaluate_node_localization.py        # **SolidiFI 层次二**节点排序评估：s_v / a_v / g_v 的
│   │                                        #   P@k / R@k / IoU + 「a_v 增量覆盖节点」→ eval_results/solidifi/
│   ├── collect_dive_comparison.py           # 三方并列汇总（① 主库内测 / ② 增强集内测 / DIVE 外部测试）→ eval_results/dive/comparison.{json,md}
│   ├── metrics.py                           # M5 指标层（已完成）：micro-F1（主）/macro-F1、每类 P/R/F1、mAP/PR-AUC、阈值扫描
│   ├── train.py                             # M5 训练（已完成）→ runs/seed{0,1,2}/（3 种子主实验已跑通，2026-09-13）；--pos-weight-cap（默认20，0=放开截断）、--loss {bce,focal,asl}（默认 bce；2026-09-14）
│   ├── evaluate.py                          # M5 评估（已完成）→ runs/seed{0,1,2}/results.json + summary.json（--summarize）
│   ├── diagnose.py                          # M5 逐类诊断（2026-09-14）→ runs/seed{0,1,2}/diagnosis.json + diagnosis_summary.json
│   └── calibrate.py                         # M5 标定补充分析（2026-09-14，仅补充不进主结果）→ eval_results/calibration/
│   # ───────────── 5.3 基线族（2026-09-22 起，decisions §46/§47；产物区见下方 eval_results/baseline/） ─────────────
│   ├── baseline_common.py                   #   三基线**共用层**：load_split/load_index/class_stats_of/
│   │                                        #   train_multilabel（含 `accum_steps` 梯度累积）/infer/search_threshold/
│   │                                        #   write_bundle/assert_bundle（7 维 + probs∈[0,1] + sample_ids 逐字）/guard_dir
│   ├── baseline_models.py                   #   三个基线模型定义（纯 nn.Module，可单测）：MVDHGRGCN / EGFLNet(+Conformer) / MandoHGT
│   ├── baseline_mvdhg_build.py              #   MVD-HG 离线：**驱动其原仓库代码**建 AST/CFG/DFG 三关系图 + 300 维 word2vec
│   │                                        #   ⚠ `read_compile` 在 `create_corpus_txt` 下会**清空**传入的 AST json ⇒ 只喂副本
│   ├── baseline_mvdhg.py                    #   MVD-HG 训练/评估（4 层 RGCN 300→64→32→16→8，num_relations=3）
│   ├── baseline_egfl_build.py               #   EGFL 离线：solc --bin → 反汇编（MANDO 的 144 项 int2op）→ 基本块 CFG → BFS 展平
│   ├── baseline_egfl.py                     #   EGFL 训练/评估（**原生字节码模态**；seq_len=512，池内 83.2% 被截断）
│   ├── baseline_mando.py                    #   MANDO-LLM 训练/评估（PyG `HGTConv` 替 dgl；无离线步，直接读正典图）
│   │                                        #   `--meta-only` 生成/校验 `hgt_metadata.json`（带 pool_sha256）
│   ├── baseline_static_tools.py             #   六个传统工具驱动（Slither 已跑；其余 5 个 2026-09-23 装好，见下）
│   ├── run_baselines.py                     #   跑批驱动（子进程 + preflight + 断点续跑）。
│   │                                        #   🔴 子进程必须 `env=dict(os.environ)`：默认继承会被父进程的 KMP 变量毒死（实测 5/5 失败）
│   ├── collect_baseline_tables.py           #   汇总 → experiments/baseline_three_caliber_tables.md
│   │                                        #   🔴 两段：**§0–§二 = §37 正典（池 453，表 1–14）**；
│   │                                        #   **「三、」= 含 buggy 新正典（池 497，表 15–28，`--with-buggy`）**。
│   │                                        #   布局由 `baseline_common.LAYOUTS` 派生（`rel_of()`），两段共用同一套渲染
│   ├── run_perclass_arm.py                  #   **7 个独立二分类器补充臂**（2026-09-23，§50）：每类一个独立模型，
│   │                                        #   回答「多标签共享是否压制了稀有类」。零模型改动——`--head binary` 的塌缩是
│   │                                        #   `any(targets)`，故把标签除第 c 列外置 0 即得该类专属分类器。
│   │                                        #   🔴 `--steps check` 逐产物断言「test 正例 = 该类 support ≠ 全类并集」，
│   │                                        #   防 7 个臂静默退化成同一个 any 分类器（那**不报错**、数字还正常）。
│   └── collect_perclass_arm.py              #   上述补充臂汇总 → experiments/perclass_arm_results.md
│   └── install_traditional_tools.sh         #   五个传统工具的环境落地（**各开独立 conda env，base 一个包都不动**，§47）
│   # 数据流原则：_pyg.pt 只读（纯结构）；_feat.pt 是 M3 唯一输出特征（dataset 只组合不再算）
│
├── # ───────────── 只读数据源（绝不写入或改名；共 5 个） ─────────────
├── MVD-HG-dataset/                # 上游源仓库（直接复制自 MVD-HG 论文）；当前不作为要使用的数据集
│   ├── {类}_contract/             #   源文件 + 合约级标签（sol_source/ + contract_labels.json：单类 0/1）
│   ├── {类}_line/                 #   行级标签（sol_source/ + contract_labels.json + solidifi/buggy 相关 json）
│   ├── {类}_contract_data_augmentation/   # 数据增强（在源文件上增加内容）后的文件 + 合约级标签
│   └── {类}_line_data_augmentation/       # 数据增强后的行级标签
│   #   7 类合约级记录共 3482 条；标签是各类漏洞分别的“0/1”指示而非 7 元组；
│   #   提炼合约级文件合并后得到 alldata(readonly)（591 源 + 2002 条 7 维标签）
│
├── alldata(readonly)/             # 主数据集（训练/验证/内部测试的唯一来源；只读）
│   ├── alldata_sol_source/        #   591 个 .sol（<项目名>/<合约名>.sol；asd_/nasd_/buggy_ 前缀；590 解析成功＝591 − 1 个 assembly>50 被过滤）
│   └── contract_labels.json       #   2002 条 7 维 multi-hot（MVD-HG 合约级标签合并；类别序锁死，reentrancy 下标 4）
│
├── alldata_augmentation/          # 并行第二数据集＝MVD-HG 论文增强集（只读；2026-09-14 置入；M1–M5 全链已跑通 2026-09-16）
│   ├── sol_source/                #   1780 个 .sol（**扁平单层**，<项目名>或 <地址>.sol；非 alldata(readonly) 的 <项目名>/ 两级布局）
│   └── contract_labels.json       #   9026 条 7 维 multi-hot（同类别序锁死，reentrancy 下标 4）；共 10 种标签模式
│   #   逐类正样本条目数：access_control 1033 / arithmetic 1204 / dos 948 / front_running 1044 /
│   #   reentrancy 971 / time_manipulation 870 / uncheck 1313；全零（无漏洞）6029 条
│   #   模式分布：0000000=6029、1111111=714、0000001 仅 uncheck=548、0100000 仅 arithmetic=439、
│   #   0001000 仅 front_running=330、1000000 仅 access_control=319、0000100 仅 reentrancy=257、
│   #   0010000 仅 dos=234、0000010 仅 time_manipulation=105、0100011 arithmetic+time+uncheck=51
│   #   谱系（decisions §19.3 已结案）：取 MVD-HG-dataset/{类}_contract_data_augmentation/，同名同内容合并
│   #   并改名去歧义（buggy_N.sol → {类}__buggy_N.sol），标签 = 7 类目录 targets==1 的并集（8997/9026 一致）。
│   #   ⚠ 标注缺陷（decisions §19.3.1）：298 个 {类}__buggy_N 文件「同名不同内容」，被并集规则推成 1111111
│   #   （714 条），与上游 solidifi_labels.json 的单类注入真值矛盾 → 全集中 59% 正样本虚高，启用前须先处置。
│   #   ⚠ 另有上游继承缺陷：123 个双地址拼接文件名、779 条幽灵标签条目（见 decisions §19.3.2）。
│
├──log.md                          # 开发手册及项目组织架构修改日志、当前进度
├── DIVE/                          # 跨数据集外部测试集（阶段 5；只读；一次性评估，不参与训练/验证/模型选择）
│   ├── Source codes/              #   22330 个 .sol
│   ├── contract_labels.json       #   21696 条 7 维（已剔除全部 634 个 Bad Randomness=1）
│   ├── DIVE_Labels.csv            #   原始标签表（9 列 8 类，含 Bad Randomness）
│   ├── Code-based.csv             #   代码级元信息（147 MB，不纳入版本管理）
│   └── Transaction-based.csv      #   交易级特征
│
├── SolidiFI/                      # 合成注入节点覆盖评估集（阶段 5 层次二；只读；只评估不训练）
│   ├── buggy_contracts/           #   350 个注入合约（1/2/4/5/6/70/71 前缀各 50）：
│   │                              #   1→access_control 2→arithmetic 4→front_running 5→reentrancy
│   │                              #   6→time_manipulation 70+71→uncheck（无 dos）
│   ├── buggy_logs/                #   350 个注入日志 CSV（注入位置/类型）
│   └── contract_labels.json       #   350 条 7 维 multi-hot（与主库同类别序）
│
├── # ───────────── 数据集产物区（按数据集分区；每区形态：raw → graphs → graphs_ft{,_aug}/ss{S} → graph_variants/ → splits/映射） ─────────────
├── products/
│   ├── alldata/                   #   主实验产物（M1–M3 已全量生成 590 图；原顶层 raw/、Heterogeneous
│   │                              #   graphs/、splits/ 于 2026-09-12 迁入此区）
│   │   ├── raw/                   #   中间产物①：解析——AST-raw/{safe}.json(590)、CFG-raw/{safe}__*.dot(25949)
│   │   │                          #   + {safe}__cfgdetail.json(590)、DFG-raw/{safe}_dfg.txt(590)、
│   │   │                          #   logs/（空=零解析错误）、filter_report.txt（过滤统计，透明性声明）
│   │   ├── graphs/                #   中间产物②+最终产物（同前缀对齐；590 图/95918 节点）**（§37 后 = 冻结编码器树，仅 `cb_frozen` 臂用；正典为 `graphs_ft/ss{S}/`）**：{safe}_hetero.json、
│   │   │                          #   _m1.json、_pyg.pt（只读结构）；{safe}_cb.pt（CodeBERT 缓存）；
│   │   │                          #   {safe}_feat.pt（**schema v2 通道字典**：struct/type_id/sv + 逐通道 sha256；融合与掩码在 model.NodeFuser）；
│   │   │                          #   legacy_feat_pre_frontend/（旧「MLP 后 128 维」特征归档，仅作 T2 回归与审计）；
│   │   │                          #   batch_summary.json（M1 批量统计）、ir_cat.json（M3 类别字典）
│   │   ├── graphs_ft/             #   **新正典（2026-09-19，decisions §37）**：微调 CodeBERT 重编码后的图
│   │   │                          #   ss{0,1,2}/（按划分种子各一套）；graph_variants/ 下是其配套的边变体
│   │   ├── graphs_ft_buggy/       #   **任务 2 的新正典（含 buggy_* 的池 497）**：`cb_ft_ss{S}/`（**注意前缀
│   │   │                          #   是 `cb_ft_`，不是 `ss`** —— `run_ablation.canonical_args` 的逐种子模板化
│   │   │                          #   必须同时认这两种形态，否则 seed1/2 会静默拿到 ss0 的编码器）。
│   │   │                          #   配套边变体在 `graph_variants/{cb_rev,cb_unlimited}_ss{S}/`，
│   │   │                          #   由 `build_ft_edge_variants.py --dataset alldata --layout buggy` 造
│   │   │                          #   （2026-09-21 新增该 layout）。边变体源与冻结树**与 §37 正典共用同一份**。
│   │   ├── baseline/              #   **5.3 三条论文基线的离线特征**（2026-09-22 起，`decisions.md` §46/§52）：
│   │   │                          #   `{mvdhg,egfl,mando}/` = canon37 段（池 453）；**`{mvdhg,egfl,mando}_buggy/`
│   │   │                          #   = 含 buggy 新正典段（池 497）**。两者**并列、绝不合并**——两个 build 都是
│   │   │                          #   「feat/<base>.pt 存在即跳过」+「w2v.model 存在即复用」，同根会让两池的
│   │   │                          #   节点特征混在一个目录里且**不报错**（`decisions.md` §52.4 第 1 条）。
│   │   │                          #   内部形态：MVD-HG 的 sol_source/AST_json/graphs/raw + 300 维 w2v、
│   │   │                          #   EGFL 的 seq/feat + 1000 词表 w2v、MANDO 只有 hgt_metadata.json。
│   │   │                          #   **整体不入库**，只留 manifest.json / hgt_metadata.json / opcodes.json 三个自述件。
│   │   ├── graph_variants/        #   **M2 边拓扑类消融的图变体**（默认留空；见 experiments/ablation_plan.md §5）：
│   │   │                          #   callback_unlimited/（--callback-limit 0）、callback_rev/（待开发）；
│   │   │                          #   _cb.pt 占 14.6/15 GB 且与边无关 → 变体软链复用，只需重建约 0.15 GB
│   │   └── splits/                #   划分（make_splits.py，默认 --strategy constrained=固定种子随机基线+覆盖约束校正）：
│   │                              #   split_seed{0,1,2}.json（train/val/test 列表）、splits.csv（seed,sample_id,split,y0..y6；1360 行 = 1 表头 + 453×3）、
│   │                              #   split_report.json（每类 pos/neg、正负比；453 池/90 buggy 剔除/47 去重/0 unmatched；
│   │                              #   逐种子×三划分逐类 support + rule_check〔C1 合计≥30% + C2 每划分每类≥1 + 两级去重不变量〕+ coverage_fix + dedup）、
│   │                              #   coverage_swaps_seed{0,1,2}.txt（覆盖校正替换清单：换入/换出合约 ID+标签向量）、
│   │                              #   dedup_dropped.txt（两级池去重丢弃清单：级别+组键+保留者）、
│   │                              #   split_metadata_seed{0,1,2}.json（策略/约束/去重参数/输入摘要/输出 sha256）、
│   │                              #   random_snapshot/（--strategy random 的隔离输出＝旧随机对照，非正典）、
│   │                              #   unmatched_contracts.txt（匹配失败与 buggy 剔除明细）、
│   │                              #   data_funnel.json（口径溯源审计报告，scripts/audit_data_funnel.py 生成）
│   ├── dive/                      #   DIVE 外部测试产物（**2026-09-19 构建**，`build_dive_external_set.py`）
│   │   ├── src_stage/             #   抽样的 **900** 个 .sol（从只读源拷入，供 M2 解析源码；只读源不改）
│   │   ├── raw/                   #   中间产物①：解析（AST/CFG/DFG）+ filter_report.txt（按数据集分别计数）
│   │   ├── graphs/                #   中间产物②+最终产物：异构图 + M3 特征（**冻结** CodeBERT = `cb_frozen` 臂用）
│   │   ├── graphs_ft/ss{S}/       #   **① 微调**编码器重编码（结构三件套软链自 graphs/）→ ① 模型的 DIVE 输入
│   │   ├── graphs_ft_aug/ss{S}/   #   **② 微调**编码器重编码 → ② 模型的 DIVE 输入
│   │   ├── graphs_ft/graph_variants/{cb_rev,cb_unlimited}_ss{S}/   # 边变体 × ① 编码器
│   │   ├── graphs_ft_aug/graph_variants/{cb_rev,cb_unlimited}_ss{S}/  # 边变体 × ② 编码器
│   │   ├── build_stage_raw.log / build_full.log   # 构建日志（按步骤打点，供审计）
│   │   └── splits/                #   外部测试子集（**已生成**）：sample_seed0.json（seed=0/**n=900**/ids/逐类支撑/停止规则）、
│   │                              #   sample_report.json（含 gate 判定：front_running=30 ≥20 闭案）
│   ├── augmentation/              #   alldata_augmentation 增强集产物（**2026-09-16 全链已生成**：1774 图 + 1774 对 M3 特征 + 两臂划分）
│   │   ├── contract_labels_repaired.json  # **正典标签**（2026-09-14 修正后；原文件在只读源，仅留痕，不得直接用）
│   │   ├── label_repair_report.json       # 修正报告（765 条改动明细 + 逐类前后计数 + solidifi 交叉校验）
│   │   ├── raw/                   #   中间产物①：解析（AST/CFG/DFG）；体积大，已 gitignore
│   │   ├── graphs/                #   中间产物②+最终产物：1774 异构图 + 1774 对 M3 特征（体积大，已 gitignore）
│   │   └── splits/                #   两臂划分：**正表 = 连通分量簇原子**（split_seed*.json 等，跨划分近重复 0/0/0）；
│   │                              #   neardup_dedup_snapshot/ = 近重复去重臂（池 1400）；
│   │                              #   near_dup_clusters{,_components}.json + leakage_audit.json（泄漏审计）
│   └── solidifi/                  #   SolidiFI 覆盖评估产物（阶段 5 待生成）
│       ├── raw/                   #   中间产物①：解析
│       ├── graphs/                #   中间产物②+最终产物：异构图 + M3 特征
│       └── mapping/               #   注入位置 → SlitherCFGNode 映射 + 无法映射样本剔除清单（数量与占比）
│
├── # ───────────── 实验产物区 ─────────────
├── runs/                          # M5 训练产物。**结果集 ①（主库）**：seed{0,1,2}/ + summary.json（2026-09-14 现行口径）；
│                                  #   **结果集 ②（增强集）**：augmentation/（正表）+ augmentation_dedup/（去重臂），2026-09-16；
│                                  #   三条例外臂：neardup/（主库零泄漏）、withbuggy/（主库含 buggy）、prior_448pool/（旧口径存档）。
│                                  #   **ablation/**：阶段 F 消融（2026-09-17 首跑 + 09-18 补 4 项 = **16 项 × 3 种子 = 48 run**；
│                                  #   结果见 experiments/ablation_results.md，计划见 ablation_plan.md）。
│                                  #   **binary_arm/**：单头二分类臂（2026-09-17，见 decisions §31）——
│                                  #   {main,aug}_{base,bin}/<cfg>_ts{T}_ss{S}/seed{T}/，n=9 同配对；
│                                  #   main_bin 与 main_base 的唯一差异 = `--head`。
│                                  #   🔴 **perclass_arm/（2026-09-23，decisions §50）**：7 个**独立二分类器**补充臂——
│                                  #   `cap{20,0}/cls_{7类名,ANY_union}/seed{0,1,2}/`，**每类一个独立模型**。
│                                  #   回答「多标签共享是否压制了稀有类」：**没有**——独立分类器在 `dos`/`front_running`/
│                                  #   `time_manipulation`（train 正例 4/2/2）上退化成「一律报无漏洞」，净效应 共享 +0.11~+0.20。
│                                  #   入库口径：**`best.pt` 排除**（45 个约 225 MB，同 `runs/ablation_n9*` 那条裁定），
│                                  #   概率缓存 + json/log 入库，全部指标可离线重算（`collect_perclass_arm.py` 从不加载权重）。
│                                  #   **prior_badmetric/**：坏指标口径全量归档（2026-09-17，含 README；见 decisions §28）。
│                                  #   另有 prior_dropout80/（保留率语义 bug）、prior_448pool/（448 池口径）两处更早归档。
│                                  #   **codebert_ft/**：§37 正典的**微调编码器**（`<语料>/ss{S}/encoder/`，2026-09-19）——
│                                  #   实测 **2.9 GB**、6 份 `pytorch_model.bin` 各 **498 MB** ⇒ **不入库**（规则见 .gitignore 段）
│                                  #   **cbft_study/**：§36 的 n=9 同配对证据（frozen vs cbft，18 run）——**原地保留，不是归档**
│                                  #   （正典 run 由它提升而来，`config.json::promoted_from` 记录来源，decisions §37）
│                                  #   **prior_frozen/**：冻结口径正典 run 的归档（含 README，decisions §37）
│                                  #   **ablation_aug/**：② 增强集的 21 臂 × 3 种子消融（2026-09-19，decisions §38）
│                                  #   🔴 **ablation_n9/ + ablation_n9_aug/（2026-09-21）**：把 21 臂的判据从 n=3 补到 **n=9**
│                                  #   （`ts × ss` 3×3 网格）。**只补非对角 6 对**——对角 3 对复用 `ablation{,_aug}/<item>/seed{S}`
│                                  #   （复用合法性 = 现行代码**逐位复现**旧 run，已由 `run_ablation_n9.reuse_violations` 逐 run 逐键对拍）。
│                                  #   叶目录形如 `<item>/ts{T}_ss{S}/seed{T}/`；① 的 9 对基线**零新跑**，取自 `cbft_study/cbft_ts{T}_ss{S}`
│                                  #   （论文正典 `runs/seed{S}` 正是它的对角提升而来）。汇总见 `experiments/ablation_n9_results.md`。
│                                  #   🔴 **两代并存，旧的绝不删（2026-09-21 用户裁定）**：n=3 的 `ablation{,_aug}/` 与它的
│                                  #   报告 `experiments/ablation_results.md`（136 KB，全文 n=3 描述性）、汇总
│                                  #   `eval_results/ablation/collected{,_aug}.{json,md}` **原地保留、不覆盖、不改写**。
│                                  #   理由：n=9 是**同配对复核**，它的价值有一半在于「和 n=3 比，哪些结论翻了」——
│                                  #   删掉 n=3 就等于删掉对照臂本身。故 n=9 一律**另开目录 + 另出报告**
│                                  #   （`ablation_n9_results.md`），两份报告的抬头各自写明口径与 n，**不得互相覆盖**。
│                                  #   对角 3 对在两处是**同一份物理产物**（n=9 靠复用而非重跑），故不存在"两个版本打架"。
│                                  #   **arch_n9/ + arch_n9_aug/（2026-09-21）**：架构基线族（GCN/GAT/SAGE，**关系盲**）的 n=9，
│                                  #   补 §40.4「要主张 RGCN 优势须 n≥9」的那一半（含参数量匹配对照 `*_pm`）。
│                                  #   🔴 **它不是 5.3 的对比方法**——大纲 5.3 列的是六个传统工具 + EGFL + MVD-HG/MANDO-LLM
│                                  #   （2026-09-21 按大纲原文重列，见 `Todo_List.md` §12.7.1）。本区只作 §40.4 内部证据。
│                                  #   ⚠ 与消融表**口径不同、必须分开读**（关系盲 + 参数量不匹配 + 须随附 parameter_report）。
│                                  #   🔴 **ablation_buggy/（2026-09-21）**：**含 `buggy_*` 的新正典（池 497）**上的
│                                  #   21 臂 × 3 种子消融，供 `per_class_three_caliber_tables_buggy.md` 的消融行。
│                                  #   与 `ablation/` **是两个正典**：`graph_dir` = `graphs_ft_buggy/cb_ft_ss{S}`、
│                                  #   划分 = `splits/withbuggy_snapshot`（test 46→49）⇒ **两边数字不可相减**。
│                                  #   臂集合与顺序取自 `run_ablation.ABLATIONS`（不另立清单，否则两表会漂移）。
│                                  #   其中 `cb_unlimited`/`cb_rev` 取自 `products/alldata/graphs_ft_buggy/graph_variants/`，
│                                  #   由 `build_ft_edge_variants.py --layout buggy` 造（见 products/ 段）。
│                                  #   error_rates.json / error_rates.md（误报率/漏报率三层口径，decisions §30；只读 `test_probs.pt`，零重训）
│                                  #   两组并存的裁定见 `experiments/decisions.md` §23、总表 `results.md` §0
│   ├── seed{0,1,2}/               #   log.txt（epoch JSONL：loss_total/loss_cls/loss_var/score_mean/score_std/
│   │                              #   val_macro_f1/…）、best.pt、last.pt、config.json（参数快照）、
│   │                              #   results.json（固定 0.5 + 验证集阈值双报告）、thresholds.json（全候选）、
│   │                              #   val_best_probs.pt（复现便利）、diagnosis.json（逐类诊断）、test_probs.pt（缓存）
│   └── summary.json               #   ≥3 种子均值 ± 标准差汇总；diagnosis_summary.json（逐类诊断跨种子聚合）
│   ├── pw_unclamped/              #   pos_weight 截断放开消融（2026-09-14，--pos-weight-cap 0）→ seed{0,1,2}/+summary+diagnosis_summary（结论见 results.md §1.8）
│   ├── loss_focal/                #   损失形状消融 focal γ=2（2026-09-14，--loss focal）→ 同上（结论见 results.md §1.10）
│   └── loss_asl/                  #   损失形状消融 ASL（2026-09-14，--loss asl）→ 同上（结论见 results.md §1.10）
├── eval_results/                  # 阶段 5 评估产物（不进入 runs/ 主结果）
│   ├── ablation/                  #   5.4.1 必要消融 11 项 + 5.4.2 可选消融 6 项
│   │                              #   ① **n=3 那份（保留，不删）**：collected.json / collected.md / collected_aug.{json,md}（2026-09-20）
│   │                              #   ② **n=9 那份（2026-09-21）**：n9_main.json / n9_aug.json / n9_summary.json
│   │                              #      （`collect_ablation_n9.py` 生成；summary 含两组、另两份是各自单组，
│   │                              #       分组文件与 summary 里对应组**逐位一致**，已机检）
│   │                              #   🔴 两代**并存、互不覆盖**：n=9 的价值有一半在「和 n=3 比哪些翻了」
│   ├── calibration/               #   标定补充分析（温度缩放 + per-class 阈值，2026-09-14；仅补充不进主结果，结论见 results.md §1.9）
│   ├── baseline/                  #   5.3 基线（**现行口径 = 大纲 5.3 表**：六个传统工具 + EGFL + MVD-HG/MANDO-LLM；
│                                  #   2026-09-21 更正——此前写的「Slither 规则/CodeBERT 序列/GCN、GAT」是旧设计，已作废。
│                                  #   **2026-09-22：三条论文基线已实现并出结果。** 每目录形如
│                                  #   `<name>/seed{S}/{test_probs.pt, val_best_probs.pt, thresholds.json,
│                                  #    results.json, config.json, log.txt, best.pt}` —— **形制与 `runs/seed{S}/`
│                                  #    完全一致**，故 `collect_three_caliber_tables.row_from_run` 可直接读（零重实现）。
│                                  #   入库口径：只入 probs 缓存 + 四个 json/log，**`best.pt` 排除**（同 `runs/**`）。
│                                  #   现状：`slither_alldata`（传统工具，**分母不同**）、`mvdhg`（驱动原码建图，
│                                  #    448/453）、`egfl`（原生字节码，453/453 但 **83.2% 序列截断到 512**）、
│                                  #    `mando`（PyG `HGTConv` 替 dgl）。离线特征在
│                                  #    `products/alldata/baseline/<name>/`（整体不入库，只留 manifest 等自述件）。
│                                  #   驱动 = `scripts/run_baselines.py`；汇总 = `scripts/collect_baseline_tables.py`
│                                  #    → `experiments/baseline_three_caliber_tables.md`。两种设定（MVD-HG 内部测试 + DIVE）
│                                  #   🔴 **2026-09-23：新增第二套正典的同一批基线**——`{mvdhg,egfl,egfl_ownlr,mando}_buggy/`
│                                  #    与 `slither_buggy/`（池 497、test 49；`decisions.md` §52）。驱动 =
│                                  #    `python scripts/run_baselines.py --layout buggy`（四条路径一起换）。
│                                  #    离线特征 = `products/alldata/baseline/<name>_buggy/`，与 canon37 的 `<name>/`
│                                  #    **并列、绝不合并**（两个 build 都是"存在即跳过"，同根会把两池混在一起且不报错）。
│                                  #    🔴 **两段跨段不可比**：test 集换了（46→49），**且**特征配对方式也变了
│                                  #    （canon37 段三种子全用 `graphs_ft/ss0`，本段用配对的 `cb_ft_ss{S}`；`_cb.pt` 逐张量随 ss 变）
│                                  #   🔴 **2026-09-23 新增第四块口径：逐类二分类 F1（binary-F1）**——见该文件的表 1
│                                  #    与 §4.1。判决阈值**逐类各一个、只在 val 上选**（复用 `calibrate.per_class_thresholds`，
│                                  #    不另写实现），把三篇论文「7 个独立二分类器」的原生形态补到三条基线上。
│                                  #    ⚠ 该口径本仓早有裁定 = **仅补充分析、不进主结果**（`metrics.py` 契约、
│                                  #    `论文开发手册.md` §1223、`decisions.md` §13）；本文方法那一行与存量
│                                  #    `eval_results/calibration/summary.json` **逐位对拍**，不一致即拒出表。
│                                  #    其 @0.5 工作点 == macro 表逐类格（恒等，故不另列）；这是该块**唯一**新增信息。
│   ├── dive/                      #   DIVE 外部测试报告：matrix_{main,aug}.json（逐臂）+
│   │                              #   comparison.{json,md}（**三方并列**；每类 PR-AUC、全零子集每类 FPR、
│   │                              #   类别先验对照）
│   └── solidifi/                  #   层次二（**2026-09-20 已跑**）：node_localization_{main_seed2,aug_seed1}.json
│                                  #   —— a_v/s_v/g_v 的 P@k/R@k/IoU（k=5/10/10%N）+ “a_v 增量覆盖节点”统计；
│                                  #   ⚠ 随机基线 P@k=0.1090，必须先减基线；结论见 decisions §40
│
├── experiments/                   # 实验决议与记录
│   ├── improvement_proposals.md   #   ① 主库指标偏低的诊断与改进建议（2026-09-20；6 视角诊断 + 对抗核查，
│   │                              #   承重条目经本人复核，逐条标 ✅/⚠️/❌；含成本与"是否作废结果"）
│   ├── improvement_round1_results.md  # 上面那份 §7 优先级表的**执行记录**（P0/P1/P3 第一轮实测；
│   │                              #   只写跑出来的数，没跑的项在 §6 显式登记为"未做"、不静默略过）
│   ├── dive_external_results.md   #   **DIVE 外部测试 + 消融三方并列**的结果与分析（2026-09-19/20；
│   │                              #   口径 2026-09-20 起 = **最佳种子为主 + mean±std 附录**，decisions §39；
│   │                              #   表 1–4 由 `scripts/collect_dive_comparison.py` 程序复算，不手抄）
│   ├── decisions.md               #   决议与口径裁定（**权威**）
│   ├── results.md                 #   实验结果记录（**权威**；数字与产物冲突时以产物为准）
│   ├── canonical_ft_numbers.md    #   **§37 微调正典的逐种子/逐类/消耗数字**——全仓新口径数字的**权威出处**（2026-09-20）
│   ├── ablation_plan.md           #   阶段 F 消融执行计划（17 项命令矩阵与就绪盘点）
│   ├── ablation_results.md        #   **阶段 F 消融结果与数据**（2026-09-17 首跑：12 项 × 3 种子，36 run）
│   │                              #   ⚠ 全文是 **n=3 描述性**，不作方向性结论（见其 §0）
│   ├── ablation_three_metric_table.md  # 三指标并列消融表（micro / macro / mAP 同表呈现，2026-09-19/20）
│   ├── cbft_paired.json           #   §36 的 n=9 同配对结果（frozen vs cbft；`paired_study_analysis.py` 生成）
│   ├── binary_main_paired.json    #   §31 二分类臂的 n=9 同配对结果（① 主库；`base` vs 各消融配置）
│   ├── binary_aug_paired.json     #   同上，② 增强集
│   ├── gcn_baseline_and_per_class_f1.md  # **七类逐类 F1 × 三工作点 + per-class 阈值 + GCN 基线**（2026-09-20）。
│   │                              #   🔴 本仓 **per-class 阈值口径的唯一权威出处**：含 §3 的**过拟合审计**
│   │                              #   （val 每类 1–3 个正样本 ⇒ 阈值极不稳定，`dos` 三种子极差 0.55；
│   │                              #    val→test 落差最大 0.16；test-oracle macro 0.80 而实际只兑现 0.636）。
│   │                              #   ⚠ 该口径**仅补充、不进主结果**（decisions §13）；GCN 一行是「关系盲」
│   │                              #   基线（忽略 `edge_type`）且**参数量不等**，两条臂在 n=3 上分不开。
│   ├── ablation_n9_results.md     #   **消融 n=9 同配对复核**（2026-09-21）：`ts × ss` 3×3 网格，
│   │                              #   `scripts/collect_ablation_n9.py` 程序生成；含「n=3 哪些结论翻了」对照、
│   │                              #   架构基线族的 n=9、参数量匹配对照、L_var 剂量臂
│   ├── cb_unlimited_reach.md      #   **`cb_unlimited` 干预触达审计**（2026-09-21，§12.4 第 2 项收尾）：
│   │                              #   逐图边集比对定触达集 → 算触达∩划分；**推翻了"Δ 被稀释、真值更大"的旧说法**
│   ├── per_class_three_caliber_tables.md        # **七类逐类 F1「三口径 × 两工作点」表**（2026-09-20，程序生成）：
│   │                              #   `collect_three_caliber_tables.py`，行 = ①正典 + ①21 臂 + ②正典 + ②21 臂 + DIVE ①②。
│   │                              #   三口径 = micro / buggy（`y.any(axis=1)` 子集）/ macro（逐类格同 micro、只换汇总列）。
│   │                              #   ⚠ 最佳种子口径无 ±；结论仍须同配对 ≥9 点。
│   ├── per_class_three_caliber_tables_buggy.md  # **同上，但正典 = 含 `buggy_*` 的新正典（池 497）**：
│   │                              #   行 = 新正典 + 新正典的 21 臂（`runs/ablation_buggy/`，2026-09-21 补）。
│   │                              #   🔴 与上一行**不是同一个 test 集**（test 46→49）⇒ **两表数字不可相减**；
│   │                              #   且含「buggy 标签假象」，引用前必读 `buggy_canon_summary.md`。
│   ├── baseline_three_caliber_tables.md         # **5.3 对比表：本文方法 vs 三条论文基线**（2026-09-22 首出，
│   │                              #   2026-09-23 扩到 16 张表；同日晚**再增「三、」段 = 含 buggy 新正典（池 497），
│   │                              #   表 15–28**——`--with-buggy` 追加，canon37 段逐字节不变）。
│   │                              #   `scripts/collect_baseline_tables.py` 程序生成，
│   │                              #   行 = 本文方法 + MVD-HG + EGFL（+ 其论文 lr 臂 `egfl_ownlr`）+ MANDO-LLM + Slither。
│   │                              #   表 1–2 = **跨口径总览**（表 1 = 逐类 binary-F1；表 2 = 方法 × 口径一览）；
│   │                              #   表 3–8 = 最佳种子明细；表 9–14 = 3 种子 mean±std。
│   │                              #   🔴 与 `per_class_three_caliber_tables.md` **同池同划分**（§37 正典，池 453），
│   │                              #   与 `..._buggy.md` **不是同一个 test 集**。引用前必读其 §0 五条声明
│   │                              #   （三条基线的实现性质不同 ⇒ 不得声称复现了作者原结果，也不得据差值谈方法优劣）。
│   └── per_class_f1.json          #   七类逐类 F1 汇总（**实际位于 `eval_results/` 下**，非本目录）
│   ├── perclass_arm_results.md    #   **7 个独立二分类器 vs 正典七维共享**（2026-09-23，§50；程序生成）：
│   │                              #   同一批图 / 同一组划分种子 / 同一套超参，只换输出头与标签。
│   │                              #   🔴 结论 = **共享在稀有类上是决定性的**：独立分类器在 `dos`/`front_running`/
│   │                              #   `time_manipulation` 上**退化成「一律报无漏洞」**（预测正例数 0/0/0，F1 ≡ 0），
│   │                              #   因 train 里这三类只有 4/2/2 个正样本；净效应 共享 **+0.11 ~ +0.20**。
│   │                              #   ⚠ 同时诚实报出唯一的反向格：最大的 `uncheck` 独立更好 **−0.084**。
│   │                              #   另含净技能对照：单头 any 分类器 +0.31 ≈ 正典七维坍缩 +0.32 ⇒ 换单头二分类无净收益。
│   ├── report_data.md             #   **数据卷**（2026-09-17）：全臂逐类 F1/阈值/计时/产物索引汇编；由 runs/ 产物直接聚合，**非权威**
│   └── report_conclusions.md      #   **结论卷**（2026-09-17）：解读、各臂处置建议、指标优化方向、待裁定事项
├── tests/                         # 27 个测试文件；`pytest tests/ -q` → **346 passed + 2 skipped**（2026-09-23 实测；`test_model_smoke.py` 占其中 22 例）
└── docs/                          # M4_interface.md（接口规范）、data_funnel.md（口径追溯报告，脚本生成）、
                                   # M3_frontend_design.md（M3 前端化设计稿，已实施）、cb_func_gapfix_plan.md（缺口修复执行计划）、
                                   # residual_gaps.md（残留缺口待办与裁定入口）、cb_func_gap{,_after}.md（缺口清单快照）
