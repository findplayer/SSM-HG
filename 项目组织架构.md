SSM-HG/
├── 论文开发手册.md / Todo_List.md / 研究点一细化大纲改I.docx
├── .gitignore                    # 忽略 __pycache__/、*.pyc、slither-env/、raw/、Heterogeneous graphs/
│                                 # （__pycache__/、.pytest_cache/ 为运行生成，未纳入版本管理）
│
├── scripts/                      # 会跑很多遍的脚本（已收拢到这，统一从仓库根运行）
│   ├── build_cfg_centered_hetero_graph.py   # M2：构建 _hetero.json（已完成）
│   ├── dump_cfg.py                          # CFG 三子类导出（已完成）
│   ├── convert_hetero_json_to_pyg.py        # M2 → _pyg.pt（已完成）
│   ├── priori_scoring.py                    # M1 规则（已完成；dos 锚点②含 push 与 array.length 自增两类无界增长模式）
│   ├── m1_runner.py                         # M1 批量打分 → _m1.json（已完成）
│   ├── m3_build_features.py                 # 已完成（M3，2026-09-05）→ _cb.pt + {safe}_feat.pt
│   │                                        #   _feat.pt = MLP 后的 128 维 h_v(0)（唯一模型输入特征，语义锁死）
│   │                                        #   不写回 _pyg.pt；--only <图前缀> 单样本验证；--variant 消融变体
│   ├── model.py                             # 已完成（M4，2026-09-07）SSMHG：RGCN/GCN 两层、h2-based Readout
│   │                                        #   forward→(z,a,node_logits)；批图 batch；helpers：apply_edge_mask/
│   │                                        #   safe_readout/validate_edge_types；GAT 拒绝；num_bases 默认 5
│   ├── dataset.py                           # 已完成（M5，2026-09-07）数据层：_pyg.pt(结构) + _feat.pt(x) + _m1(s_v)
│   │                                        #   + 标签对齐(加载断言) + 边级消融开关（不再过 MLP）
│   ├── metrics.py                           # 待实现（M5）指标层：宏/微 F1、每类 P/R/F1、mAP
│   ├── make_splits.py                       # 已完成（M5，2026-09-07）→ splits/split_seed{seed}.json
│   │                                        #   + split_report.json + unmatched_contracts.txt
│   ├── train.py                             # 待实现（M5）→ runs/seedN/{log.txt, best.pt, config.json}
│   ├── evaluate.py                          # 待实现（M5）→ runs/seedN/results.json
│   │                                        #   --task ablation/baseline → eval_results/
│   └── generate_all_ast_cfg_dfg.sh          # 批量生成 raw（已改好）
│   # 数据流原则：_pyg.pt 只读（纯结构）；_feat.pt 是 M3 唯一输出特征；dataset.py 只组合不再算
│
├── raw/                          # 全量解析原始产物（已生成 581 个合约）
│   ├── AST-raw/  {safe}.json
│   ├── CFG-raw/  {safe}__*.dot , {safe}__cfgdetail.json
│   ├── DFG-raw/  {safe}_dfg.txt
│   ├── logs/      ast_error.log / cfg_error.log / dfg_error.log（日志与产物分离；当前为空=全量解析无错误）
│   └── filter_report.txt                     # 过滤统计（论文透明性声明用）
│
├── Heterogeneous graphs/         # 全部图产物，同名前缀对齐（hetero/m1/pyg/_cb/_feat 各 581 已生成 + 2 个 feat 消融变体）
│   ├── {safe}_hetero.json        # M2 修订版
│   ├── {safe}_m1.json            # M1
│   ├── batch_summary.json        # M1 批量统计（含 total_class_hits / class_hit_graphs 七类聚合）
│   ├── ir_cat.json               # M3 类别字典：IR≤20 类 / 可见性4 / 外呼方式5（A3 已生成 2026-09-05）
│   ├── {safe}_pyg.pt             # 只读结构：edge_index/edge_type/node_id/meta/label（x 为 N×1 占位，永不改写）
│   ├── {safe}_cb.pt              # M3 CodeBERT 缓存（手册 8.4.3）
│   ├── {safe}_feat.pt            # M3 唯一输出：MLP 后的 128 维 h_v(0)（语义锁死；dataset 只组合不重算，可覆盖重跑）
│   └── {safe}_feat_{variant}.pt  # M3 消融变体输出（--variant no-prior/no-codebert，复用 _cb.pt 缓存）
│
├── splits/                       # M5（make_splits.py 生成）
│   ├── split_seed{0,1,2}.json    #   train/val/test 合约名列表
│   ├── split_report.json         #   每类正负样本数/正负比/唯一合约/多标签/全 0 统计（手册 10.2.4）
│   └── unmatched_contracts.txt   #   标签匹配不上的合约（记录并报告，不静默丢弃，手册 10.2.2）
├── runs/                         # M5 主实验（3 种子；待生成）
│   ├── seed{0,1,2}/              #   log.txt / best.pt / results.json（固定 0.5 + 搜索阈值双报告）
│   │                             #   config.json（本次运行参数快照，实验可追踪）
│   └── summary.json              #   ≥3 种子均值 ± 标准差汇总
├── eval_results/                 # 仅阶段 5：消融 + 基线 + SmartBugs/SolidiFI 结果（待生成：M5）
├── experiments/                  # M5 实验决议与记录
│   └── decisions.md              #   v5（2026-09-08）：划分/损失/消融/复现口径决议
│
├── tests/                        # M4 起（test_model_smoke.py：22 用例，pytest/独立运行均可）
├── docs/                         # M4 起（M4_interface.md：forward/返回/批图/关系编号/helper 接口规范）
├── slither-env/                  # 空 venv（已 .gitignore，勿用；实际解析环境为 conda base）
├── alldata(readonly)/            # 只读：alldata_sol_source/ + contract_labels.json（已有）
├── MVD-HG-dataset/               # 只读：单类标签核对用（已有）
├── SolidiFI/                     # 只读外部数据集（阶段 5 层次二：合成注入节点覆盖评估，只评估不训练）
│   ├── buggy_contracts/          #   350 个注入合约：前缀 1/2/4/5/6/70/71 各 50
│   │                             #   映射：1→access_control 2→arithmetic 4→front_running
│   │                             #         5→reentrancy 6→time_manipulation 70+71→uncheck
│   ├── buggy_logs/               #   350 个注入日志 CSV（{prefix}_BugLog_{n}.csv：注入位置/类型记录）
│   └── contract_labels.json      #   350 条 7 维 multi-hot（与主库同类别序，评估对齐用）
└── DIVE/                         # 只读外部数据集（阶段 5 泛化评估：MVD-HG 训练 → DIVE 测试）
    ├── Source codes/             #   22330 个 .sol（{id}.sol；另含 .DS_Store 非数据条目）
    ├── contract_labels.json      #   21696 条 7 维 multi-hot（与主库同类别序）=22330 源中剔除全部
    │                             #   634 个 Bad Randomness=1 合约后的结果（八类→七类，被剔除者不参与评估）
    ├── DIVE_Labels.csv           #   原始标签表（9 列：8 类；第 8 类 Bad Randomness 即上述剔除判定依据）
    ├── Code-based.csv            #   代码级元信息（contractID/ABI/ContractName/CompilerVersion/…）
    └── Transaction-based.csv     #   交易级特征（contractID/NoOfTransactions/…）

# 状态（2026-09-07）：M1/M2 审计收尾关闭；M3 完成（ir_cat.json、_cb.pt×581、_feat.pt×581）；M4 完成——scripts/model.py + tests/test_model_smoke.py（22 用例全绿）+ docs/M4_interface.md，simple_dao 真实前向验收通过；M5 划分落地——dataset.py + make_splits.py 已实现，splits/ 已生成（train 396/val 50/test 49×3 种子；495 训练池/86 buggy 剔除/0 unmatched）；下一步 M5（metrics.py → train.py → evaluate.py）。
# 外部数据集就位（2026-09-11）：新增只读 SolidiFI/（350 注入合约 + 350 注入日志 + 350 条 7 维标签）与 DIVE/（22330 源码 + 21696 条 7 维标签 + DIVE_Labels/Code-based/Transaction-based 三张原始 CSV）。DIVE 的 contract_labels.json 已剔除全部 634 个 Bad Randomness=1 合约（22330-634=21696），标签已是七类口径，评估时直接用，无需再处理第 8 类。二者均只用于阶段 5 评估（SolidiFI=层次二节点覆盖、DIVE=泛化），不参与训练；使用前须先建类别映射表（SolidiFI 前缀→七类已确定，手册 10.5/10.6）。
# 目录一致性核对（2026-09-11）：逐目录比对实际文件系统——已核实 raw/（AST 581 / CFG 25253 / DFG 581 / raw/filter_report.txt；logs/ 当前为空=解析无错误）、Heterogeneous graphs/（hetero/m1/pyg/_cb/_feat 各 581 + 2 个消融变体 + batch_summary.json/ir_cat.json）、splits/（5 文件）、alldata(readonly)/（591 源 + contract_labels.json）、MVD-HG-dataset/ 均与本文一致；补记 experiments/decisions.md、slither-env/、.gitignore；标注 runs/、eval_results/、metrics.py/train.py/evaluate.py 为待生成/待实现（M5）。

